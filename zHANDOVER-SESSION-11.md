# zHANDOVER-SESSION-11

## REPOSITORY

- Branch: main
- Prior commits (cantrip workstream):
  - `<new>` — Cantrip-10: Implement Gust, Primal Savagery, True Strike, Resistance, Magic Stone (XGE/PHB)
  - `abf347d` — Cantrip-9: Implement Infestation, Word of Radiance, Create Bonfire, Produce Flame, Shillelagh (XGE/PHB)
  - `fe8cec1` — Cantrip-8: Implement Frostbite, Green-Flame Blade, Lightning Lure, Sword Burst, Sapping Sting (XGE/TCE/EGW)
  - `c4cfc11` — Cantrip-7: Implement Eldritch Blast, Toll the Dead, Mind Sliver, Thunderclap, Booming Blade (PHB/XGE/TCE)
  - `c975049` — Cantrip-6: Implement Fire Bolt, Acid Splash, Poison Spray, Vicious Mockery, Sacred Flame (PHB)
  - `bc4d033` — Cantrip-3/4/5: Recover lost sessions (Chill Touch + Blade Ward + handovers)
  - `f2f40a3` — Spell-cache: per-level cache + batch picker tooling
- Commits this session:
  - `<new>` — Cantrip-10: Implement Gust, Primal Savagery, True Strike, Resistance, Magic Stone (XGE/PHB)
- URL: https://github.com/mcabel/dnd-combat-sim
- PAT: user provides in chat each session. do not warn.

---

## ⚠️ WORKSTREAM OWNERSHIP — READ FIRST

| File family | Workstream | Owner |
|---|---|---|
| `zHANDOVER-SESSION-*.md` | **Cantrips** | **THIS agent (you)** |
| `HANDOVER-SESSION-*.md` | Core Engine / leveled spells | other agent — DO NOT TOUCH |
| `SHEET-HANDOVER-*.md` | Character Sheet UI | other agent — DO NOT TOUCH |

### Your priorities (cantrip workstream)

- **Your tasks come from `zHANDOVER-SESSION-*.md`.** This handover defines the next batch.
- Implement cantrips per PHB/XGE/TCE/EGW (2014 canon, pre-2024 only). Sessions 1–10 implemented 30 cantrips. This session extends to the **next batch of 5 cantrips** (curated for mechanical diversity — utility/healing-adjacent batch, since the remaining 16 in-scope cantrips are mostly utility with no combat damage).
- Reuse the cantrip architecture in `src/engine/cantrip_effects.ts` — now **FOUR registries** (Session 10 added `Gust` to `CANTRIP_EFFECTS`, `True Strike` + `Resistance` to `CANTRIP_SELF_EFFECTS`; `Primal Savagery` + `Magic Stone` are metadata-only):
  - `CANTRIP_EFFECTS` (13) — post-hit / post-save-fail riders: Thorn Whip, Ray of Frost, Shocking Grasp, Chill Touch, Vicious Mockery, Mind Sliver, Booming Blade, Frostbite, Sapping Sting, Lightning Lure, Green-Flame Blade, Infestation, **Gust**
  - `CANTRIP_ATTACK_ADVANTAGE` (1) — pre-roll advantage: Shocking Grasp
  - `CANTRIP_SELF_EFFECTS` (4) — non-attack self-buffs: Blade Ward, Shillelagh, **True Strike**, **Resistance**
  - `CANTRIP_AOE_EFFECTS` (3) — caster-centered AoE: Thunderclap, Sword Burst, Word of Radiance
- Do NOT create a `case 'spellName'` in `executePlannedAction` for cantrips.
- Do NOT touch sheet routes, `leveler.ts`, or `builder.ts` (TASK.md constraint).

---

## STARTUP CHECKLIST (do these before implementing)

1. `git pull` — make sure you're on the latest `main`.
2. Read **`zHANDOVER-SESSION-10.md`** (Session 10 patterns: Gust's `pushAway(caster, target, state)` for push-AWAY forced movement — distinct from Thorn Whip / Lightning Lure's pull-TOWARD and Infestation's RANDOM direction, with a canon Medium-or-smaller size constraint; Primal Savagery's metadata-only melee spell attack pattern (mirror Fire Bolt but melee/acid/1d10); True Strike's `_trueStrikeAdvNextAttack` scratch field + advantage fold-in at resolveAttack's attack-roll branch + one-shot consume — distinct from Shocking Grasp's same-turn-pre-roll advantage and Frostbite's weapon-only disadvantage; Resistance's `_resistanceDieBonusNextSave` scratch field + +1d4 ADD in `rollSave` (mirror Mind Sliver's -1d4 SUBTRACT, opposite sign) + one-shot consume; Magic Stone's metadata-only ranged spell attack pattern with `1d6 + spellcastingMod` bludgeoning damage and `damageBonusField: 'spellcastingMod'` metadata flag — no scaling). These are your templates.
3. Read `zHANDOVER-SESSION-9.md` (Infestation's `rollRandomDirection()` + `isDestinationBlocked()` helpers, Word of Radiance's `CANTRIP_AOE_EFFECTS` registration, Shillelagh's `_shillelaghActive` scratch field, Produce Flame's metadata-only THROW-mode v1 simplification, Create Bonfire's on-cast-only v1 simplification) — also templates.
4. Read `zHANDOVER-SESSION-8.md` (Frostbite's weapon-attack-only filter, Sapping Sting's `addCondition` for prone, Lightning Lure's `action.damage = null` for conditional damage, Green-Flame Blade's `findSplashTarget` + `rollSplashDamage`, Sword Burst's `CANTRIP_AOE_EFFECTS`) — also templates.
5. Read `zHANDOVER-SESSION-7.md` (Mind Sliver's `rollSave` integration, Booming Blade's `executeMove` movement hook, Thunderclap's `CANTRIP_AOE_EFFECTS` registry, Toll the Dead's conditional damage, Eldritch Blast's multi-beam v1 simplification) — also templates.
6. Read `SPELL-CACHE.md` — it explains the cache + picker workflow.
7. Run `npm install` (deps: ts-node, typescript).
8. Run `npm run spell-cache:build` — refresh the cache (confirm 30/49 cantrips implemented).
9. For each cantrip below, run `npm run spell-cache:show -- "Name"` to get the full raw 5etools JSON (entries, scalingLevelDice, components) before implementing.

---

## GOALS THIS SESSION — Batch: 5 cantrips (utility / healing-adjacent)

Picked from `spell-cache/level-0.json` (curated for mechanical diversity — the remaining 16 in-scope cantrips are mostly utility with no combat damage; this batch picks the 5 that exercise a NEW pattern or extend an existing one). These 5 push the cantrip architecture into new territory: a stabilize-downed-ally heal cantrip (Spare the Dying), an ability-check +1d4 buff (Guidance — mirror Resistance for ability checks), a CHA-check advantage buff (Friends — mirror True Strike for ability checks), a persistent-light utility (Light — forward-compat for the darkness-vision subsystem), and an illusion utility (Minor Illusion — forward-compat for the cover/distract subsystem).

| # | Name | School | Effect | Source | Page | Module to create |
|---|------|--------|--------|--------|------|------------------|
| 1 | **Spare the Dying** | Necromancy | touch · instant · **stabilize a living creature at 0 HP** (no effect on undead or constructs) · v1 simplification: touch range requires the caster to be adjacent to the downed ally (canon: touch range); v1 does NOT model the undead/construct exclusion (forward-compat TODO) | PHB (2014-08-19) | 277 | `src/spells/spare_the_dying.ts` |
| 2 | **Guidance** | Divination | touch · 1 min concentration · **target adds 1d4 to one ability check of its choice before the spell ends** · v1 simplification: 1-round duration, self-only (canon: 1 min concentration, any willing creature); MIRROR Resistance's `_resistanceDieBonusNextSave` scratch field but for ABILITY CHECKS instead of SAVES (requires a new `rollAbilityCheck` choke point in utils.ts) | PHB (2014-08-19) | 248 | `src/spells/guidance.ts` |
| 3 | **Friends** | Enchantment | self · 1 min concentration · **advantage on all CHA checks directed at one creature of your choice that isn't hostile toward you** · v1 simplification: 1-round duration, target-agnostic (canon: 1 min concentration, vs one non-hostile creature); MIRROR True Strike's `_trueStrikeAdvNextAttack` scratch field but for CHA CHECKS instead of ATTACK rolls | PHB (2014-08-19) | 244 | `src/spells/friends.ts` |
| 4 | **Light** | Evocation | touch · 1 hour · **object sheds bright light in 20-ft radius + dim light for additional 20 ft** · v1 simplification: implement only the on-cast effect (set a `_lightSourceActive` flag on the target); skip the persistent-light-radius integration (the engine does not yet model light-radius-based vision changes — forward-compat TODO via `lightVisionIntegrationV1Implemented: false`) | PHB (2014-08-19) | 255 | `src/spells/light.ts` |
| 5 | **Minor Illusion** | Illusion | 30 ft · 1 min · **create a sound or an image of an object within range that lasts for the duration** · v1 simplification: implement only the on-cast effect (set an `_illusionActive` flag on the battlefield); skip the INT (Investigation) check to disbelieve (the engine does not yet model illusions-as-cover or illusions-as-distractions — forward-compat TODO via `illusionMechanicsV1Implemented: false`) | PHB (2014-08-19) | 260 | `src/spells/minor_illusion.ts` |

### Implementation checklist (paste into the session's completion notes)

- [ ] **Spare the Dying** (`PHB p.277`) — Touch range, instant, V+S (no M). **Effect: stabilize a living creature at 0 HP** (the creature stops making death saves; it's no longer dying but still at 0 HP and unconscious). This is the FIRST heal-adjacent cantrip — mirrors Healing Word's stabilize logic but as a CANTRIP (no slot cost) and touch range (vs Healing Word's 60 ft ranged). v1 simplification: implement only the stabilize effect (set `target.isStabilized = true` and clear any pending death-save failures/successes); skip the undead/construct exclusion (canon: "This spell has no effect on undead or constructs." — forward-compat TODO via `spareTheDyingTypeExclusionV1Implemented: false`). Components: V + S (no M). NO CANTRIP_EFFECTS entry (not a post-hit rider — Spare the Dying has no attack roll and no save; it's a direct touch-effect on a willing/downed target). NO CANTRIP_SELF_EFFECTS entry (the target is NOT the caster). **NEW routing:** Spare the Dying is a non-attack, non-AoE, non-self-buff cantrip that targets a DOWNED ALLY — this is a NEW pattern. Route via a NEW registry `CANTRIP_TOUCH_EFFECTS` in `cantrip_effects.ts` (consulted by `executePlannedAction` AFTER `CANTRIP_SELF_EFFECTS` and BEFORE `CANTRIP_AOE_EFFECTS`; the handler takes `(caster, target, state)` and returns true if the touch effect was applied). Mirror the executePlannedAction routing for CANTRIP_SELF_EFFECTS — Spare the Dying bypasses resolveAttack entirely (no attack roll, no save). The handler checks `target.currentHP === 0 && target.isPlayer && !target.isDead` (only downed PCs can be stabilized — monsters die at 0 HP per PHB p.197); sets `target.isStabilized = true`, clears `target.deathSaves.failures` and `target.deathSaves.successes`, emits a "stabilized" log. If the target is NOT at 0 HP (or is a monster), the spell fizzles (no effect, but the action is consumed). No scratch fields needed (the stabilize effect is instant — modifies target state directly). No cleanup needed.
- [ ] **Guidance** (`PHB p.248`) — Touch range, 1 min concentration (v1: 1-round non-concentration), V+S (no M). **Effect: target adds 1d4 to one ability check of its choice before the spell ends.** This is the FIRST ability-check-bonus cantrip. MIRRORS Resistance's `_resistanceDieBonusNextSave` scratch field but for ABILITY CHECKS instead of SAVES. NEW scratch field on `Combatant`: `_guidanceDieBonusNextAbilityCheck?: number` (set to 4 on cast — d4; consumed by `rollAbilityCheck` in utils.ts on the next ability check, mirror how Resistance is consumed by `rollSave`). **NEW CHOKE POINT:** `rollAbilityCheck(combatant, ability, dc)` in utils.ts — this function does NOT yet exist in the engine. If the engine has no `rollAbilityCheck` function (search for it first), v1 simplification: register Guidance in `CANTRIP_SELF_EFFECTS` (mirror Resistance — set the flag on cast, clear on cleanup), and document the missing `rollAbilityCheck` integration as TODO via `guidanceAbilityCheckIntegrationV1Implemented: false`. The flag is still set on the target; future work adds the `rollAbilityCheck` choke point and consumes the flag there. Components: V + S (no M). v1 simplification flags: `guidanceConcentrationV1Simplified: true` (canon: 1 min conc; v1: 1 round) AND `guidanceTouchAllyV1Simplified: true` (canon: any willing creature; v1: self-only).
- [ ] **Friends** (`PHB p.244`) — Self, 1 min concentration (v1: 1-round non-concentration), S+M (a small amount of makeup applied to the face as this spell is cast). **Effect: advantage on all CHA checks directed at one creature of your choice that isn't hostile toward you.** This is the SECOND advantage-granting self-buff cantrip (after True Strike). MIRRORS True Star's `_trueStrikeAdvNextAttack` scratch field but for CHA CHECKS instead of ATTACK ROLLS. NEW scratch field on `Combatant`: `_friendsAdvNextChaCheck?: boolean` (set on cast, consumed by the future `rollAbilityCheck` choke point on the next CHA check). Like Guidance, v1 may not have a `rollAbilityCheck` choke point — if not, register Friends in `CANTRIP_SELF_EFFECTS` (mirror True Strike — set the flag on cast, clear on cleanup), and document the missing integration as TODO via `friendsAbilityCheckIntegrationV1Implemented: false`. v1 simplification flags: `friendsConcentrationV1Simplified: true` (canon: 1 min conc; v1: 1 round) AND `friendsTargetAgnosticV1Simplified: true` (canon: vs one non-hostile creature; v1: target-agnostic) AND `friendsHostilityBacklashV1Implemented: false` (canon: "the creature realizes that you used magic to influence its mood and becomes hostile toward you" — v1 skips this backlash).
- [ ] **Light** (`PHB p.255`) — Touch range, 1 hour, V+M (a firefly or phosphorescent moss). **Effect: object sheds bright light in 20-ft radius + dim light for additional 20 ft.** If the object is held/worn by a hostile creature, that creature makes a DEX save to avoid the spell. v1 simplification: implement only the on-cast effect (set a `_lightSourceActive?: boolean` scratch field on the target object/creature; emit a "begins to glow with light" log). Skip the persistent-light-radius integration (the engine's `computeLOS` does not yet model light-radius-based vision changes — Darkness and similar effects also lack this integration; forward-compat TODO via `lightVisionIntegrationV1Implemented: false`). The hostile-creature DEX save IS implemented in v1 (mirror Sacred Flame's save branch — Light's save DC = caster's spell save DC, save ability = 'dex'). Components: V + M (a firefly or phosphorescent moss — NO S). v1 simplification flags: `lightVisionIntegrationV1Implemented: false` (engine lacks light-radius vision) AND `lightDismissalV1Implemented: false` (canon: "dismiss it as an action" — v1 has no dismissal action) AND `lightRecastEndsPreviousV1Implemented: false` (canon: "The spell ends if you cast it again" — v1 has no recast-tracking). NO CANTRIP_EFFECTS entry (Light is not a post-hit rider — it's a save-based spell on hostile targets, OR a direct touch-effect on willing targets). NO CANTRIP_SELF_EFFECTS entry (Light targets an OBJECT, not the caster). NO CANTRIP_AOE_EFFECTS entry (Light is not a caster-centered AoE — the light RADIUS is a vision effect, not a damage AoE). **Routing:** Light routes through resolveAttack's save branch (for hostile targets — the DEX save avoids the spell) OR a direct touch-effect (for willing targets — no save, just apply the light flag). v1 simplification: treat ALL targets as "willing" (no DEX save) for simplicity; document the hostile-target DEX save as TODO via `lightHostileTargetSaveV1Simplified: true`. This makes Light a metadata-only module (mirror Create Bonfire v1 — provide metadata only, the AI/parser builds an Action with `attackType: 'special'` and no damage, the cantrip routes through a new `CANTRIP_TOUCH_EFFECTS` registry similar to Spare the Dying).
- [ ] **Minor Illusion** (`PHB p.260`) — 30 ft, 1 min, S+M (a bit of fleece). **Effect: create a sound or an image of an object within range that lasts for the duration.** The image can't create sound, light, smell, or any other sensory effect; physical interaction reveals it as an illusion. A creature can use its action to examine the illusion and determine it's an illusion with a successful INT (Investigation) check vs the caster's spell save DC. v1 simplification: implement only the on-cast effect (emit a "creates an illusion" log; no scratch fields, no persistent-illusion tracking). Skip the INT (Investigation) check to disbelieve (the engine has no `rollAbilityCheck` choke point yet — same blocker as Guidance/Friends). Skip the illusion-as-cover integration (the engine's `computeLOS` does not yet model illusions-as-cover — forward-compat TODO via `illusionCoverIntegrationV1Implemented: false`). Components: S + M (a bit of fleece — no V). v1 simplification flags: `illusionMechanicsV1Implemented: false` (no Investigation check, no cover integration, no physical-interaction reveal) AND `illusionSoundVsImageV1Simplified: true` (canon: choose sound OR image; v1: emit a single "illusion" log without distinguishing). NO CANTRIP_EFFECTS / SELF / AoE entries (Minor Illusion v1 is metadata-only — the AI/parser builds an Action with `attackType: 'special'` and no damage; the cantrip routes through a new `CANTRIP_ILLUSION_EFFECTS` registry OR is handled inline as a metadata-only "flavor" cantrip with no mechanical effect). **SIMPLEST v1:** treat Minor Illusion as a metadata-only "flavor" cantrip (like Primal Savagery / Magic Stone but with NO attack roll and NO damage — just emit an "illusion created" log via `resolveCantripAction` returning true with a single log event). This avoids needing a new registry.

### Integration points you will touch (expected)

- `src/types/core.ts`: Add `_guidanceDieBonusNextAbilityCheck?: number` to `Combatant` (Guidance). Add `_friendsAdvNextChaCheck?: boolean` to `Combatant` (Friends). Add `_lightSourceActive?: boolean` to `Combatant` (Light — forward-compat for the vision subsystem). Add `_isStabilized?: boolean` to `Combatant` (Spare the Dying — flag for the death-saves subsystem to read; OR use the existing `deathSaves` field with a `stabilized: true` sub-flag — check the existing death-saves implementation in `combat.ts`/`utils.ts` first). Minor Illusion needs NO new scratch fields (v1 is metadata-only with no persistent state).
- `src/engine/cantrip_effects.ts`: Add a NEW registry `CANTRIP_TOUCH_EFFECTS` for non-attack, non-AoE, non-self-buff cantrips that target a DOWNED ALLY or willing creature (Spare the Dying, Light v1). The handler signature is `(caster, target, state) => boolean` (mirror `CANTRIP_EFFECTS` but called BEFORE resolveAttack, like `CANTRIP_SELF_EFFECTS`). Register `'Spare the Dying'` and `'Light'` in `CANTRIP_TOUCH_EFFECTS`. Register `'Guidance'` and `'Friends'` in `CANTRIP_SELF_EFFECTS` (mirror Resistance / True Strike — both are self-buffs that set a scratch flag for a future choke point). Minor Illusion: register in `CANTRIP_SELF_EFFECTS` (v1 treats it as a flavor-only self-buff that emits a single log event — no target required).
- `src/engine/combat.ts`:
  - `executePlannedAction` `case 'cast':` — add a NEW consultation of `CANTRIP_TOUCH_EFFECTS` (via a new `resolveCantripTouchEffect(caster, target, actionName, state)` function in `cantrip_effects.ts`) AFTER `resolveCantripAction` (self-buffs) and `resolveCantripAoE` (AoE), but BEFORE the target-null guard. The touch-effect handler receives the target as an argument (unlike self-buffs which only take the caster). If it returns true, the switch breaks (the touch cantrip bypasses resolveAttack).
  - `resolveAttack` attack branch — no changes needed (Guidance and Friends are for ability checks, not attack rolls; True Star already covers attack advantage; Light has no attack roll; Minor Illusion has no attack roll; Spare the Dying has no attack roll).
  - `executeMove` — no changes needed.
- `src/engine/utils.ts`:
  - `resetBudget` — add `cleanupGuidance(c)`, `cleanupFriends(c)`, `cleanupLight(c)` (clear the flags if not consumed before the start of the caster's next turn — v1 simplification, see checklists). Spare the Dying and Minor Illusion need NO cleanup (Spare the Dying's stabilize is instant; Minor Illusion v1 has no persistent state).
  - `rollSave` — no changes needed (Guidance and Friends are for ability checks, not saves — Resistance already covers the save-bonus integration).
  - **FUTURE WORK (out of scope for this batch):** add a `rollAbilityCheck(combatant, ability, dc)` function (mirror `rollSave`'s architecture — fold in `_guidanceDieBonusNextAbilityCheck` and `_friendsAdvNextChaCheck`). The engine currently has NO `rollAbilityCheck` choke point; v1 sets the flags but does not consume them (documented via the v1 simplification flags).
- **Do NOT** touch the AI planner (`src/ai/planner.ts`) — that's Core Engine territory.

### Tests (write one `*.test.ts` per cantrip, in the repo's ts-node convention)

Mirror `src/test/gust.test.ts` (74 tests — push-away forced movement + size constraint + blocked destination + Booming Blade control), `src/test/primal_savagery.test.ts` (57 tests — vanilla melee spell attack + acid damage type + scaling metadata), `src/test/true_strike.test.ts` (49 tests — self-buff + advantage on next attack + any-attack-type + one-shot consume + resetBudget cleanup), `src/test/resistance.test.ts` (49 tests — self-buff + +1d4 to next save + one-shot consume + mirror Mind Sliver opposite-sign), and `src/test/magic_stone.test.ts` (61 tests — vanilla ranged spell attack + bludgeoning + no scaling + spellcastingMod bonus) for structure. Use deterministic save outcomes (DC=30 for guaranteed fail, DC=1 + ability=30 for guaranteed success) and `isCritOverride=true` for forced attack-roll hits. Target ~50–80 tests per cantrip. Each test file must exit non-zero on failure (`process.exit(1)`).

**Critical test cases per cantrip:**
- Spare the Dying: metadata; components (V+S — no M); touch range (0 ft); **stabilize downed PC** (set `currentHP=0`, `isPlayer=true`, `isUnconscious=true`, cast Spare the Dying → verify `isStabilized=true` and deathSaves cleared); **no effect on monsters at 0 HP** (canon: monsters die at 0 HP, not get stabilized — verify the handler fizzles); **no effect on a creature above 0 HP** (verify the handler fizzles, action consumed); **no effect on undead/constructs** (v1 simplification — TODO via flag, but verify the canon text is documented); **CANTRIP_TOUCH_EFFECTS routing** (verify the cantrip bypasses resolveAttack — no attack hit/miss/damage events); **DEX save NOT triggered** (Spare the Dying has no save — touch range, willing target); v1 simplification flag (`spareTheDyingTypeExclusionV1Implemented: false`); resolveCantripAction returns false (NOT a self-buff); resolveCantripAoE returns false (NOT a caster-centered AoE); dispatcher safety.
- Guidance: metadata; components (V+S — no M); touch range (0 ft); **self-buff flag** (isSelfBuff = true); **+1d4 to next ability check** (set `_guidanceDieBonusNextAbilityCheck = 4` on caster); **buff is one-shot** (consumed on the first ability check — IF `rollAbilityCheck` exists; otherwise, document the missing integration); **buff clears at start of caster's next turn** (resetBudget cleanup, mirror Blade Ward / Shillelagh / True Strike / Resistance); **v1 simplification flags** (`guidanceConcentrationV1Simplified: true` AND `guidanceTouchAllyV1Simplified: true` AND `guidanceAbilityCheckIntegrationV1Implemented: false`); **mirror Resistance's architecture** (same die size 4 = d4, same one-shot consume semantics — but for ability checks instead of saves); applySelfEffect sets flag + emits log; resolveCantripAction integration; dispatcher safety; resolveCantripAoE returns false.
- Friends: metadata; components (S+M — makeup, no V); self range (0 ft); **self-buff flag** (isSelfBuff = true); **advantage on next CHA check** (set `_friendsAdvNextChaCheck = true` on caster); **buff is one-shot** (consumed on the first CHA check — IF `rollAbilityCheck` exists; otherwise, document the missing integration); **buff clears at start of caster's next turn** (resetBudget cleanup); **v1 simplification flags** (`friendsConcentrationV1Simplified: true` AND `friendsTargetAgnosticV1Simplified: true` AND `friendsHostilityBacklashV1Implemented: false` AND `friendsAbilityCheckIntegrationV1Implemented: false`); **mirror True Strike's architecture** (same one-shot consume semantics — but for CHA checks instead of attack rolls); applySelfEffect sets flag + emits log; resolveCantripAction integration; dispatcher safety; resolveCantripAoE returns false.
- Light: metadata; components (V+M — firefly/phosphorescent moss, NO S); touch range (0 ft); **1-hour duration** (v1 simplification: 1-round, clears at start of caster's next turn); **sets `_lightSourceActive` flag on target** (forward-compat for the vision subsystem); **v1 simplification flags** (`lightVisionIntegrationV1Implemented: false` AND `lightDismissalV1Implemented: false` AND `lightRecastEndsPreviousV1Implemented: false` AND `lightHostileTargetSaveV1Simplified: true`); **CANTRIP_TOUCH_EFFECTS routing** (verify the cantrip bypasses resolveAttack — no attack hit/miss/damage events); v1 treats ALL targets as willing (no DEX save — TODO); resolveCantripAction returns false (NOT a self-buff); resolveCantripAoE returns false (NOT a caster-centered AoE); dispatcher safety.
- Minor Illusion: metadata; components (S+M — bit of fleece, no V); 30 ft range; **1-minute duration** (v1 simplification: 1-round, clears at start of caster's next turn — though v1 has no persistent state to clear); **v1 is metadata-only with no mechanical effect** (emit a single "creates an illusion" log via `resolveCantripAction`); **v1 simplification flags** (`illusionMechanicsV1Implemented: false` AND `illusionSoundVsImageV1Simplified: true`); **CANTRIP_SELF_EFFECTS routing** (v1 treats it as a self-buff that emits a flavor log — no target required); resolveCantripAoE returns false; dispatcher safety; **NO scratch fields** (v1 has no persistent state); **NO new Combatant fields** (v1 has no scratch fields); **flavor log emitted on cast** (verify a single "illusion created" action event is logged, with NO attack hit/miss/damage events).

---

## COMPLETED THIS SESSION (Session 10, for reference)

### Feature: 5 cantrips implemented

1. **Gust** (`src/spells/gust.ts`) — XGE p.157. STR save, 30 ft, NO damage (the cantrip has no damage dice — it's a pure control effect that pushes the target 5 ft away). **Rider: on save-FAIL, target is pushed 5 ft AWAY from the caster** (deterministic direction — distinct from Infestation's random d4). This is the THIRD cantrip with forced movement (Thorn Whip / Lightning Lure pull TOWARD; Infestation moves RANDOM; Gust pushes AWAY). Reuses the forced-movement pattern (direct `target.pos` set, NOT `executeMove` — no OAs, no Booming Blade detonation, mirror Thorn Whip / Lightning Lure / Infestation). NEW HELPER: `pushAway(caster, target, state)` pushes the target 5 ft directly away from the caster along the line connecting their positions. If the destination is blocked (reuse Infestation's `isDestinationBlocked` helper — already exported), the target doesn't move. **CANON SIZE CONSTRAINT:** XGE p.157 says "One Medium or smaller creature" — Gust has a Medium-or-smaller size restriction (unlike Infestation which has no size constraint, and unlike Thorn Whip / Lightning Lure which have Large-or-smaller). Implemented via `canPushSize(combatant)` helper and `GUST_PUSHABLE_SIZES = ['Tiny', 'Small', 'Medium']`. Components: V + S (CANON — 5etools JSON `{"v":true,"s":true}` — NO material component; the Session 10 handover mentioned "a legume seed" but that's not in the 5etools JSON for XGE p.157 — this module follows the canon JSON per the Session 9 protocol of always cross-checking the handover against the 5etools spell-cache JSON). Register in `CANTRIP_EFFECTS` (post-save-FAIL). No scratch field needed (movement is instant). No cleanup needed. **CANON NOTE:** XGE p.157 has THREE modes: Mode 1 (utility: push an unattended 5-ft object 5 ft away — no save), Mode 2 (combat: STR save vs push on a creature), Mode 3 (harmless sensory effect — flavor only). v1 implements ONLY Mode 2 (the combat mode); Modes 1 and 3 are documented as TODO via the metadata flag `gustUtilityModeV1Implemented: false`.
2. **Primal Savagery** (`src/spells/primal_savagery.ts`) — XGE p.163. Melee spell attack, 1d10 acid, reach 5 ft (range: Self), scales at 5/11/17 to 2d10/3d10/4d10. v1 simplification: implement as a vanilla melee spell attack (mirror Fire Bolt but melee instead of ranged, acid instead of fire, 1d10 same dice). Components: S only (no V, no M — XGE p.163: "Components: S"). The cantrip's flavor ("you grow claws") has no mechanical effect beyond the attack/damage — no rider. **No CANTRIP_EFFECTS/SELF/AoE entries** (metadata only). Acid damage is RARE on cantrips — only Acid Splash (PHB p.211) also deals acid. Good for damage-type coverage testing. Mirror the Fire Bolt test structure (43 tests) but adapt for melee + acid (57 tests). Metadata exposes `reachFt: 5` for the AI/parser to distinguish melee spell attacks from ranged spell attacks (both use `attackType: 'spell'` in this engine).
3. **True Strike** (`src/spells/true_strike.ts`) — PHB p.284. Self-buff, 1 round (v1; canon: concentration, up to 1 minute). **Rider: advantage on the FIRST attack roll on the caster's NEXT turn.** v1 simplification: the buff is consumed on the first attack roll the caster makes AFTER casting True Strike. This is the SECOND advantage-granting cantrip (after Shocking Grasp, which grants advantage on the SAME turn vs metal armor). Distinct from Shocking Grasp: True Star grants advantage on a LATER turn's first attack, regardless of target. NEW scratch field on `Combatant`: `_trueStrikeAdvNextAttack?: boolean` (set on cast, consumed by resolveAttack's attack-roll branch on the FIRST attack the caster makes, then cleared). Components: S only (CANON — 5etools JSON `{"s":true}` — NO material component; the Session 10 handover mentioned "a small piece of sheep's wool" which is the canonical 2014 PHB component, but the 5etools JSON for the in-scope pre-2024 source lists S only — this module follows the canon JSON per the Session 9 protocol). Register in `CANTRIP_SELF_EFFECTS` (mirror Blade Ward / Shillelagh — non-attack self-buff). The advantage integration is in resolveAttack's attack-roll branch: add `attacker._trueStrikeAdvNextAttack === true` to the `advantage` boolean (mirror how Vicious Mockery / Frostbite are folded into `disadvantage`), then consume (set to false) after the roll resolves — one-shot. Cleanup via `resetBudget` (clears at start of caster's next turn — but canonically the buff is consumed by the first attack, so cleanup is a safety net). **v1 simplification flags:** `trueStrikeConcentrationV1Simplified: true` (canon: concentration up to 1 minute; v1: 1 round) AND `trueStrikeTargetAgnosticV1Simplified: true` (canon: "first attack roll against the target"; v1: target-agnostic — applies to next attack regardless of target).
4. **Resistance** (`src/spells/resistance.ts`) — PHB p.272. Self-buff (touch range — can target self or ally), 1 round (v1; canon: concentration, up to 1 minute). **Rider: target adds 1d4 to one saving throw it makes before the spell ends.** This is the FIRST save-bonus self-buff cantrip. MIRRORS Mind Sliver's `_mindSliverDiePenaltyNextSave` scratch field but as a BUFF on an ALLY instead of a DEBUFF on an enemy — same architecture, opposite sign (Mind Sliver SUBTRACTS 1d4; Resistance ADDS 1d4). NEW scratch field on `Combatant`: `_resistanceDieBonusNextSave?: number` (set to 4 on cast — d4; consumed by `rollSave` in utils.ts on the next save, mirror how Mind Sliver is consumed). Components: V + S + M (a miniature cloak — CANON — 5etools JSON `{"v":true,"s":true,"m":"a miniature cloak"}`). Register in `CANTRIP_SELF_EFFECTS` (mirror Blade Ward / Shillelagh — non-attack self-buff). The save-bonus integration is in `rollSave` (utils.ts): if `_resistanceDieBonusNextSave` is set, roll 1d4 and ADD to the save total (mirror Mind Sliver's subtract-1d4 logic), then consume. Cleanup via `resetBudget` (clears at start of caster's next turn — but canonically the buff is consumed by the first save, so cleanup is a safety net). **v1 simplification flags:** `resistanceConcentrationV1Simplified: true` (canon: concentration up to 1 minute; v1: 1 round) AND `resistanceTouchAllyV1Simplified: true` (canon: any willing creature; v1: self-only).
5. **Magic Stone** (`src/spells/magic_stone.ts`) — XGE p.160. Ranged spell attack, 1d6 + spellcastingMod bludgeoning, 60 ft throw range (CANON — XGE p.160: "If thrown, a pebble has a range of 60 feet." The Session 10 handover listed "30 ft throw range" — 30 ft is the SLING range per PHB p.149; 60 ft is the THROW range per canon. This module follows the canon 60 ft throw range per the Session 9 protocol of always cross-checking the handover against the 5etools spell-cache JSON). NO scaling (Magic Stone is one of the few cantrips that does NOT scale at 5/11/17 — the 1d6 + spellcastingMod is flat at all levels). v1 simplification: implement only the SELF-THROW mode (the caster throws their own pebbles as a ranged spell attack). The ALLY-THROW mode (the cantrip enchants 1-3 pebbles that ALLIES can throw as ranged spell attacks using the CASTER's spell attack mod) is documented as TODO via the metadata flag `magicStoneAllyThrowV1Implemented: false`. Mirror Fire Bolt (PHB p.242) — same pattern: ranged spell attack, V+S (no M — the pebbles are the TARGET of the spell, not a material component). CANON casting time: BONUS ACTION (XGE p.160: `time:[{number:1,unit:"bonus"}]` — one of the few bonus-action cantrips, like Shillelagh). v1 simplification: collapse the bonus-action enchant + action throw into a single ACTION (the built Action has `costType: 'action'`, not `'bonusAction'`) — documented via `magicStoneBonusActionEconomyV1Simplified: true`. v1 simplification: persistent enchantment (canon: 1-minute duration, multiple throws) → v1: each throw is a fresh cast + throw — documented via `magicStonePersistentEnchantmentV1Simplified: true`. Damage formula: `1d6 + spellcastingMod` bludgeoning (the +spellcastingMod is part of the canon damage — XGE p.160: "the target takes bludgeoning damage equal to 1d6 + your spellcasting ability modifier"). The AI/parser reads the `damageBonusField: 'spellcastingMod'` metadata flag to know the Action's `damage.bonus` should be set to the caster's spellcastingMod. **No CANTRIP_EFFECTS/SELF/AoE entries** (metadata only for v1).

### Integration points touched (Session 10)

- `src/types/core.ts`: Added `_trueStrikeAdvNextAttack?: boolean` to `Combatant` (True Strike). Added `_resistanceDieBonusNextSave?: number` to `Combatant` (Resistance). Gust, Primal Savagery, Magic Stone need NO new scratch fields (Gust's push is instant; Primal Savagery and Magic Stone are metadata-only).
- `src/engine/cantrip_effects.ts`: Added `'Gust'` to `CANTRIP_EFFECTS` (post-save-FAIL). Added `'True Strike'` and `'Resistance'` to `CANTRIP_SELF_EFFECTS` (non-attack self-buffs). (Primal Savagery and Magic Stone need NO registry entries — they're metadata-only.) Updated module header to list all 21 supported cantrips across 4 registries.
- `src/engine/combat.ts`:
  - **`resolveAttack` attack-roll branch** — added True Strike advantage: if `attacker._trueStrikeAdvNextAttack === true`, fold into the `advantage` boolean (mirror Frostbite's `_frostbiteDisadvNextWeaponAttack` but for advantage instead of disadvantage, and NOT restricted by attackType — True Star applies to ANY attack roll: melee, ranged, AND spell). The `advantage` variable was moved from line 239 to AFTER the True Strike flag is read (line 322) so the flag can be folded in. Consume (set to false) after the attack roll resolves — one-shot. The consume block is placed alongside Vicious Mockery / Frostbite one-shot consumes (lines 367-376).
  - **`resolveAttack` save branch** — NO changes needed (Gust auto-routes via `applyCantripEffect` post-save-FAIL; True Strike and Resistance auto-route via `resolveCantripAction` for self-buffs, bypassing resolveAttack entirely).
  - **`executePlannedAction` `case 'cast':`** — NO changes needed (Gust, True Strike, and Resistance all auto-route via the existing registries).
  - **`executeMove`** — NO changes needed (Gust's push is forced movement via direct pos set, bypassing executeMove — mirror Thorn Whip / Lightning Lure / Infestation).
- `src/engine/utils.ts`:
  - `rollSave` — added Resistance integration: if `combatant._resistanceDieBonusNextSave` is set, roll 1d4 and ADD to the save total (mirror Mind Sliver's subtract-1d4 logic, opposite sign), then consume (set to undefined). The save total formula became `roll + mod + prof + biBonus + blessBonus + wbBonus - mindSliverPenalty + resistanceBonus`.
  - `resetBudget` now calls `cleanupTrueStrike(c)` (clears `_trueStrikeAdvNextAttack` if not consumed before the start of the caster's next turn — v1 1-round simplification) and `cleanupResistance(c)` (clears `_resistanceDieBonusNextSave` if not consumed before the start of the caster's next turn — v1 1-round simplification).

### Tests (Session 10)

- `src/test/gust.test.ts`: 74 tests. Metadata, STR save, components (V+S — CANON, no M — 5etools JSON `{"v":true,"s":true}`, contradicts the handover's "legume seed" claim), moveDistanceFt = 5, maxSize = Medium (canon size constraint — distinct from Infestation's no-constraint), v1 simplification flag (Mode 1 utility TODO), no scaling, null damageDice (pure control), **canPushSize() — Medium and smaller return true; Large+ return false**, **pushAway() deterministic direction AWAY from caster** (EAST push from caster-at-5,5/target-at-7,5 → target moves to 8,5; NORTH push from caster-at-5,5/target-at-5,3 → target moves to 5,2), **target ends up FARTHER from caster than before** (6 test cases including diagonal — distAfter > distBefore), **blocked destination → no push** (wall at destination), **Large+ target NOT pushed** (size constraint, canon), **same-position degenerate case → no push**, **applyCantripEffect module — applies push on save-FAIL**, **dispatcher integration — 'Gust' registered in CANTRIP_EFFECTS**, dispatcher safety, **cleanup is a no-op** (push is instant), **save FAIL → target pushed 5 ft AWAY (50 iterations — allMovedAway + allValidDistance)**, **save SUCCESS → NO push**, **forced movement does NOT trigger Booming Blade** (control test — flag still set, HP unchanged, no BB detonation log), **no damage dealt** (action.damage is null — Gust is pure control), Total Cover respects, **push direction is AWAY (distinct from Thorn Whip / Lightning Lure TOWARD)**.
- `src/test/primal_savagery.test.ts`: 57 tests. Metadata, scaling (1d10/2d10/3d10/4d10 at 5/11/17), damageType = acid (rare — only Acid Splash also deals acid), components (S only — no V, no M), reachFt = 5 (melee spell attack), rangeFt = 0 (Self), no CANTRIP_EFFECTS/SELF/AoE entries, dispatcher safety, Action shape (attackType = spell, reach = 5, damageType = acid), **resolveAttack HIT → acid damage** (crit 2d10 = 2..20 range), **resolveAttack MISS → no damage**, **acid damage type control test** (mentions "acid", NOT "fire"), Total Cover respects.
- `src/test/true_strike.test.ts`: 49 tests. Metadata, components (S only — CANON, no M — 5etools JSON `{"s":true}`, contradicts the handover's "sheep's wool" claim), isSelfBuff = true, riderAttackTypes = ['melee', 'ranged', 'spell'] (any attack type), v1 simplification flags (concentration + target-agnostic), does NOT scale, **applySelfEffect sets _trueStrikeAdvNextAttack flag + emits log**, resolveCantripAction integration, dispatcher safety, **cleanup clears flag (resetBudget integration)**, **advantage on next attack roll** (melee attack with buff — verify "Advantage (True Strike)" log emitted + flag consumed + "fades" consume log), **buff applies to ANY attack type** (melee, ranged, AND spell — all 3 get advantage), **buff is one-shot** (consumed on first attack; second attack does NOT get advantage), **buff clears at start of next turn** (resetBudget), **True Star bypasses resolveAttack** (self-buff — only 1 action event, no attack/damage events), **not a caster-centered AoE**, **no CANTRIP_EFFECTS entry** (not a post-hit rider).
- `src/test/resistance.test.ts`: 49 tests. Metadata, components (V+S+M — miniature cloak), isSelfBuff = true, riderDieSides = 4 (d4), v1 simplification flags (concentration + touch-ally), does NOT scale, **applySelfEffect sets _resistanceDieBonusNextSave = 4 + emits log**, resolveCantripAction integration, dispatcher safety, **cleanup clears flag (resetBudget integration)**, **rollSave adds 1d4 bonus** (save total in 6..28 with wis=18 + +1d4; without bonus would be 5..24), **rider CONSUMED after one save (one-shot)**, **second save has NO bonus (one-shot consume verified)**, **buff clears at start of next turn** (resetBudget), **Resistance bypasses resolveAttack** (self-buff), **not a caster-centered AoE**, **no CANTRIP_EFFECTS entry**, **mirror Mind Sliver architecture, opposite sign** (Resistance +1d4 ADD = 2..24 with wis=10; Mind Sliver -1d4 SUBTRACT = -3..19 with wis=10; both flags CONSUMED after the save).
- `src/test/magic_stone.test.ts`: 61 tests. Metadata, damageType = bludgeoning (pebbles deal bludgeoning), components (V+S — no M, pebbles are target not component), castingTime = bonusAction (canon — XGE p.160), throwRangeFt = 60 (canon — XGE p.160, NOT 30 ft sling range), v1 simplification flags (ally-throw TODO + bonus-action-economy collapsed + persistent-enchantment skipped), does NOT scale (1d6+mod flat at all levels — verified by 20-iteration crit damage test at level 1 AND level 17 both in 5..15 range), damageBonusField = 'spellcastingMod', no CANTRIP_EFFECTS/SELF/AoE entries, dispatcher safety, Action shape (attackType = spell, range.normal = 60, damage.bonus = 3 spellcastingMod, costType = action v1), **resolveAttack HIT → bludgeoning damage** (crit 2d6 + 3 spellcastingMod = 5..15 range — the +mod is FLAT, not doubled per PHB p.196), **resolveAttack MISS → no damage**, **bludgeoning damage type control test** (mentions "bludgeoning", NOT "fire"), Total Cover respects, **no scaling — metadata has NO scalingLevels/scalingDice fields** (distinct from Fire Bolt / Primal Savagery which DO have them).

**Total new tests: 290.** All use deterministic save outcomes (DC=30 / DC=1+ability=30) and `isCritOverride=true` for forced attack hits (avoids the 5% nat-1 flakiness documented in prior handovers).

---

## DISCOVERIES / PATTERNS FROM THIS SESSION (reuse these)

1. **Four cantrip registries now well-populated.** 13 cantrips in `CANTRIP_EFFECTS` (added Gust), 1 in `CANTRIP_ATTACK_ADVANTAGE`, 4 in `CANTRIP_SELF_EFFECTS` (added True Star + Resistance), 3 in `CANTRIP_AOE_EFFECTS`. The architecture is flexible enough for any cantrip pattern. The Session 11 batch will add a FIFTH registry `CANTRIP_TOUCH_EFFECTS` for non-attack, non-AoE, non-self-buff cantrips that target a DOWNED ALLY or willing creature (Spare the Dying, Light v1). Guidance and Friends will be added to `CANTRIP_SELF_EFFECTS` (self-buffs with a scratch flag for a future `rollAbilityCheck` choke point). Minor Illusion v1 will be added to `CANTRIP_SELF_EFFECTS` as a flavor-only self-buff (no mechanical effect in v1).
2. **Push-away forced movement pattern (Gust — Session 10 implemented).** For cantrips that push the target AWAY from the caster (Gust): use the same forced-movement pattern (direct pos set, no executeMove, no OAs, no Booming Blade) but compute the push direction as AWAY from the caster (target.pos + (target.pos - caster.pos) normalized * pushDist, rounded to the nearest grid cell). Distinct from Thorn Whip / Lightning Lure (pull TOWARD caster) and Infestation (RANDOM direction). Reuse Infestation's `isDestinationBlocked` helper for the blocked-destination check. **CANON SIZE CONSTRAINT:** Gust has a Medium-or-smaller size restriction (XGE p.157: "One Medium or smaller creature") — implemented via `canPushSize(combatant)` and `GUST_PUSHABLE_SIZES = ['Tiny', 'Small', 'Medium']`. This is distinct from Thorn Whip / Lightning Lure (Large-or-smaller) and Infestation (no constraint). Future cantrips that push targets should follow this size-constraint pattern.
3. **Advantage-on-next-attack self-buff pattern (True Star — Session 10 implemented).** For self-buffs that grant advantage on the caster's NEXT attack roll (True Star): set a scratch flag on the caster in `applySelfEffect` (registered in CANTRIP_SELF_EFFECTS). In `resolveAttack`'s attack-roll branch, check the flag and fold it into the `advantage` boolean (mirror Frostbite's `_frostbiteDisadvNextWeaponAttack` but for ADVANTAGE instead of DISADVANTAGE, and NOT restricted by attackType — True Star applies to ANY attack roll: melee, ranged, AND spell). Consume (set to false) after the attack roll resolves — one-shot. **IMPORTANT: the `advantage` variable must be computed AFTER the True Star flag is read** (the original line 239 `const advantage = baseAdv || ... || cantripAdv;` was moved to line 322, after the True Star flag is read at line 316, so the flag can be folded in). Cleanup via `resetBudget` → `cleanup`. Distinct from Shocking Grasp (same-turn pre-roll advantage, read by CANTRIP_ATTACK_ADVANTAGE) and Frostbite (target debuff, weapon-only, read by the `disadvantage` boolean).
4. **Save-bonus self-buff pattern (Resistance — Session 10 implemented).** For self-buffs that grant +1d4 to the target's NEXT save (Resistance): set a scratch flag on the caster in `applySelfEffect` (registered in CANTRIP_SELF_EFFECTS). The flag value is the die size (4 = d4) so the system is extensible. In `rollSave` (utils.ts), check the flag, roll rollDie(value), ADD to the save total, then consume (set to undefined). Mirror Mind Sliver's subtract-1d4 logic but with the OPPOSITE SIGN (Resistance ADDS, Mind Sliver SUBTRACTS). Cleanup via `resetBudget` → `cleanup`. **The save total formula became** `roll + mod + prof + biBonus + blessBonus + wbBonus - mindSliverPenalty + resistanceBonus`. Future save-bonus cantrips (e.g. a hypothetical "Greater Resistance" that adds 1d6) set the flag to 6 and reuse this integration.
5. **Metadata-only melee spell attack pattern (Primal Savagery — Session 10 implemented).** For cantrips whose v1 implementation needs NO rider and is a vanilla melee spell attack (Primal Savagery: 1d10 acid, 5-ft reach, scales): provide `metadata` only. The AI/parser builds an Action from metadata with `attackType: 'spell'` (melee spell attacks use 'spell' in this engine — the `reach` field distinguishes them: reach=5 for melee spell, range.normal=120 for ranged spell). Add a `reachFt: 5` metadata field for the AI/parser to read. NO CANTRIP_EFFECTS/SELF/AoE entries. The cantrip routes through resolveAttack's standard attack-roll branch. v1 simplifications are documented via metadata flags. Acid damage is RARE on cantrips — only Acid Splash (PHB p.211) and Primal Savagery (XGE p.163) deal acid. Good for damage-type coverage testing.
6. **Metadata-only ranged spell attack with spellcastingMod damage bonus (Magic Stone — Session 10 implemented).** For cantrips whose v1 implementation is a vanilla ranged spell attack WITH a +spellcastingMod damage bonus (Magic Stone: 1d6 + spellcastingMod bludgeoning, 60 ft throw range, NO scaling): provide `metadata` only. Add a `damageBonusField: 'spellcastingMod'` metadata field so the AI/parser knows the Action's `damage.bonus` should be set to the caster's spellcastingMod (NOT zero like Fire Bolt's flat 1d10). NO CANTRIP_EFFECTS/SELF/AoE entries. The cantrip routes through resolveAttack's standard attack-roll branch. The +spellcastingMod bonus is FLAT (not doubled on crit — PHB p.196: only dice are doubled, not flat bonuses). v1 simplifications: ally-throw mode TODO (the cantrip enchants 1-3 pebbles that ALLIES can throw using the CASTER's spell attack mod — needs a persistent-enchantment subsystem), bonus-action economy collapsed (canonically bonus-action enchant + action throw → v1 single action with costType='action'), persistent enchantment skipped (canon: 1-minute duration, multiple throws → v1: each throw is a fresh cast + throw). Documented via metadata flags.
7. **CANON CORRECTION: Gust components are V+S (no M); True Star components are S only (no M).** The Session 10 handover listed Gust as "V + S + M (a legume seed)" and True Star as "S + M (a small piece of sheep's wool)", but the 5etools spell-cache JSON for the in-scope pre-2024 sources lists Gust as `{"v":true,"s":true}` (no M) and True Star as `{"s":true}` (no M). This module follows the canon JSON (V+S for Gust, S only for True Star) per the Session 9 protocol of always cross-checking the handover's component list against the 5etools spell-cache JSON before implementing. Run `npm run spell-cache:show -- "Name"` to see the raw JSON. **Future cantrip sessions should ALWAYS cross-check the handover's component list (and save ability, and range, and damage) against the 5etools spell-cache JSON before implementing** — the handover is a planning doc, the 5etools JSON is the authoritative source.
8. **CANON CORRECTION: Magic Stone throw range is 60 ft (not 30 ft); damage is 1d6 + spellcastingMod (not 1d6 flat); casting time is bonus action (not action).** The Session 10 handover listed "30 ft (throw range)" and "1d6 bludgeoning" without explicitly calling out the bonus-action casting time. The 5etools spell-cache JSON for XGE p.160 says: "If thrown, a pebble has a range of 60 feet" (60 ft throw, not 30 ft sling); "the target takes bludgeoning damage equal to 1d6 + your spellcasting ability modifier" (+spellcastingMod); "time":[{"number":1,"unit":"bonus"}] (bonus action). This module follows the canon JSON. Future cantrip sessions should ALWAYS cross-check the handover's range/damage/casting-time claims against the 5etools spell-cache JSON.
9. **`advantage` variable placement in resolveAttack (combat.ts).** The `advantage` boolean was originally computed at line 239 (BEFORE the Vicious Mockery / Frostbite / True Star flag reads at lines 286-316). When adding a new advantage source that depends on a scratch flag read AFTER line 239 (like True Star), the `advantage` variable MUST be moved to AFTER the flag is read — otherwise the flag won't be folded in. Session 10 moved `const advantage = ...` from line 239 to line 322 (after the True Star flag is read at line 316). The `baseAdv / cantripAdv / packTacticsAdvantage / helpedThisTurn` components are still pre-computed at line 238 (those don't depend on scratch flags). Future cantrip sessions adding a new advantage source that depends on a scratch flag should follow this pattern — move the `advantage` computation to after the flag is read, OR pre-compute the flag at line 238 (before the `advantage` line).
10. **Pre-existing flaky tests (do NOT try to fix — outside cantrip scope):** `combat.test.ts` (varying pass count 52–54, occasional 1 failure — d20-probabilistic), `faerie_fire.test.ts` (occasional 2 failures under suite load), `burning_hands.test.ts` (10% failure rate on nat 1-2). **Also previously observed:** `arms_of_hadar.test.ts` (occasional 2 failures, d20-probabilistic), `rage.test.ts` (occasional 1 failure, d20-probabilistic). All verified pre-existing by re-running on the Session 9 codebase without re-running cantrip tests — flakiness persists. NOT caused by cantrip work. The handover explicitly says do NOT fix these. **All 5 pre-existing flaky tests PASSED on the Session 10 final regression run** (77 test files, ~4646+ tests, all green).
11. **Build hygiene:** run `./node_modules/.bin/tsc --noEmit` before committing. Run the full suite: `for t in src/test/*.test.ts; do timeout 75 ./node_modules/.bin/ts-node --transpile-only "$t" || echo "FAIL: $t"; done` (77 files, ~4646+ tests, must stay green except for the documented pre-existing flakiness). Revert test side-effects before committing: `git checkout -- characters/` if any fixture JSON got an `updatedAt` bump. **Session 10 followed this protocol:** characters/ reverted (one fixture JSON got an `updatedAt` bump from test side-effects — reverted before commit), all 77 test files green.
12. **`eq()` takes 3 args (label, a, b), `assert()` takes 3 args (label, cond, detail).** Do NOT pass a detail string to `eq()` — it only takes `label, a, b` and the failure detail is auto-generated from JSON.stringify. Use `assert()` if you need a custom detail string on failure. The pattern `assert('label', x?.includes('Y') === true, true)` is WRONG — the third arg should be a string detail. Use `assert('label', x?.includes('Y') === true, 'got: ...')` or just `assert('label', !!x?.includes('Y'), 'got: ...')`.
13. **Save-fail log casing (resolveAttack vs cantrip modules):** `resolveAttack`'s save branch (combat.ts) logs save_fail/save_success with LOWERCASE ability name (e.g. "fails DC 30 dex save") because it uses `${action.saveAbility}` (which is lowercase 'dex'/'con'/'str' etc.). Cantrip MODULES that emit their OWN save logs (Sword Burst, Thunderclap, Word of Radiance) use UPPERCASE ability name (e.g. "fails DEX save"). When testing a metadata-only cantrip that goes through resolveAttack's save branch (Create Bonfire, Acid Splash, Poison Spray, Gust, etc.), check for LOWERCASE. When testing a cantrip MODULE that emits its own save log (Sword Burst, Thunderclap, Word of Radiance), check for UPPERCASE. To avoid casing bugs, use `.toLowerCase().includes('dex')` for case-insensitive matching.
14. **`isCritOverride=true` for deterministic attack-roll hits in tests.** +20 hitBonus vs AC 10 still has a 5% nat-1 auto-miss rate (PHB p.194). Use `isCritOverride=true` (5th arg to `resolveAttack`) to force a hit. Crit doubles the damage dice — adjust the damage range assertion accordingly (1d8 → 2d8 = 2..16, 1d10 → 2d10 = 2..20, 1d6 → 2d6 = 2..12). `isCritOverride=false` forces a miss. This is the attack-roll analogue of "DC=30 for guaranteed save fail." Established in Session 7, reused throughout Sessions 8/9/10. **Magic Stone adds a +spellcastingMod FLAT bonus to damage — this is NOT doubled on crit (PHB p.196: only dice are doubled, not flat bonuses). Adjust the crit damage range accordingly: 1d6 + 3 spellcastingMod → 2d6 + 3 = 5..15 on crit (NOT 2d6 + 6 = 8..18).**
15. **`JSON.stringify` for `eq()` deep equality on objects.** The `eq()` function uses `JSON.stringify` for the failure detail, but `===` for the actual comparison. For primitive comparisons (string, number, boolean), `===` works. For object comparisons, use `JSON.stringify(a) === JSON.stringify(b)` explicitly, or use `assert()` with a custom detail string. The pattern `eq('label', JSON.stringify(caster), before)` (used in the Gust cleanup no-op test) works because both sides are stringified. For object equality, prefer `assert('label', objA.field === objB.field, 'detail')` over `eq('label', objA, objB)`.

---

## IMMEDIATE NEXT ACTION

1. `git pull && npm install && npm run spell-cache:build` (confirm 30/49 cantrips implemented).
2. Implement the 5 cantrips in the order above (Spare the Dying first — new CANTRIP_TOUCH_EFFECTS registry; Guidance second — mirror Resistance for ability checks; Friends third — mirror True Star for CHA checks; Light fourth — metadata-only with `_lightSourceActive` flag; Minor Illusion last — metadata-only flavor log).
3. After each: `tsc --noEmit` + run that cantrip's test.
4. After all 5: run the **full regression suite** (must stay green; pre-existing flakiness in combat/faerie_fire/burning_hands/arms_of_hadar/rage is documented and NOT caused by cantrip work).
5. `npm run spell-cache:build` again — confirm cantrip implemented count goes 30 → 35.
6. Commit: `Cantrip-11: Implement Spare the Dying, Guidance, Friends, Light, Minor Illusion (PHB)`.
7. Write `zHANDOVER-SESSION-12.md` (next batch — use `npm run spell-cache:pick -- --level 0 --count 5` to choose, or curate for mechanical diversity).
8. **Push to GitHub.** Tell the user explicitly if the push fails.

---

## TEST STATUS (before this session — i.e. AFTER session 10)

- `gust.test.ts`: 74/74 · `primal_savagery.test.ts`: 57/57 · `true_strike.test.ts`: 49/49 · `resistance.test.ts`: 49/49 · `magic_stone.test.ts`: 61/61
- Prior cantrip tests still green: `fire_bolt.test.ts` 43/43, `acid_splash.test.ts` 44/44, `poison_spray.test.ts` 46/46, `vicious_mockery.test.ts` 47/47, `sacred_flame.test.ts` 51/51, `blade_ward.test.ts` 38/38, `chill_touch.test.ts` 38/38, `shocking_grasp.test.ts` 26/26, `thorn_whip.test.ts` 11/11, `eldritch_blast.test.ts` 53/53, `toll_the_dead.test.ts` 61/61, `mind_sliver.test.ts` 48/48, `thunderclap.test.ts` 54/54, `booming_blade.test.ts` 218/218, `frostbite.test.ts` 57/57, `sword_burst.test.ts` 54/54, `sapping_sting.test.ts` 50/50, `lightning_lure.test.ts` 88/88, `green_flame_blade.test.ts` 209/209, `word_of_radiance.test.ts` 58/58, `produce_flame.test.ts` 52/52, `infestation.test.ts` 277/277, `shillelagh.test.ts` 60/60, `create_bonfire.test.ts` 99/99
- Full regression suite (77 files, ~4646+ tests): all green EXCEPT 5 pre-existing flaky tests (verified by re-running on the Session 10 codebase — all 5 PASSED on the final run, but they're documented as occasionally flaky):
  - `combat.test.ts`: varying pass count 52–54, occasional 1 failure (d20-probabilistic)
  - `faerie_fire.test.ts`: occasional 2 failures under suite load (d20-probabilistic)
  - `burning_hands.test.ts`: 10% failure rate on nat 1-2 (documented in prior handovers)
  - `arms_of_hadar.test.ts`: occasional 2 failures (d20-probabilistic)
  - `rage.test.ts`: occasional 1 failure (d20-probabilistic)
- `tsc --noEmit`: 0 errors
- Spell cache: 557 unique spells, **30/49 cantrips implemented**, 16 cantrips remaining in-scope (3 out-of-scope XPHB-only: Elementalism, Sorcerous Burst, Starry Wisp)

---

## NOTES FOR NEXT AGENT

- **Scope rule (per user):** canon pre-2024; reprints → newest in-scope source wins; XPHB (2024) out of scope. The cache handles this automatically — trust `spell-cache:pick`'s canonical-source column.
- **Creatures follow a different rule** (all variants kept) — not applicable to spells. See `SPELL-CACHE.md`.
- **Parser tech debt** (still open, documented in zHANDOVER-3/4/5/6/7/8/9/10): `hasMetalArmor` and `isUndead` flags exist on `Combatant` but aren't populated by the parser. `spellcastingMod` and `casterLevel` fields also exist but aren't populated by the parser. Not blocking this batch.
- **AI planner** does not yet select most cantrips — engine routing is enough for this batch. AI selection is a Core Engine task.
- **Commit message convention:** `Cantrip-N: <summary>` (this session was Cantrip-10; next session is Cantrip-11).
- **Pre-existing flaky tests** (do NOT try to fix — outside cantrip scope, verified pre-existing): `combat.test.ts`, `faerie_fire.test.ts`, `burning_hands.test.ts`, `arms_of_hadar.test.ts`, `rage.test.ts`. These are d20-probabilistic and NOT caused by cantrip work.
- **Architecture summary (4 cantrip registries, 21 cantrips total after Session 10):**
  - `CANTRIP_EFFECTS` (13) — post-hit / post-save-fail riders: Thorn Whip, Ray of Frost, Shocking Grasp, Chill Touch, Vicious Mockery, Mind Sliver, Booming Blade, Frostbite, Sapping Sting, Lightning Lure, Green-Flame Blade, Infestation, Gust
  - `CANTRIP_ATTACK_ADVANTAGE` (1) — pre-roll advantage: Shocking Grasp
  - `CANTRIP_SELF_EFFECTS` (4) — self-buffs: Blade Ward, Shillelagh, True Strike, Resistance
  - `CANTRIP_AOE_EFFECTS` (3) — caster-centered AoE: Thunderclap, Sword Burst, Word of Radiance
  - (Primal Savagery + Magic Stone are metadata-only — no registry entry.)
- **Exported for testability:** `resolveAttack` (Session 6), `executeMove` (Session 7), `rollRandomDirection` / `directionToDelta` / `isDestinationBlocked` / `applyRandomMove` (Session 9 — Infestation), `pushAway` / `canPushSize` (Session 10 — Gust). All have doc comments noting they're for direct testing of cantrip engine integration.
- **New `Combatant` fields added in Session 10:** `_trueStrikeAdvNextAttack?: boolean`, `_resistanceDieBonusNextSave?: number`. Optional with sensible defaults.
- **Push-away forced movement architecture (Gust):** For push-AWAY moves, compute the direction vector as (target.pos - caster.pos), normalize, scale by 1 grid square (5 ft), round to the nearest integer grid cell. Check `isDestinationBlocked` (off-battlefield OR wall via `segmentIntersectsAABB`), set `target.pos` directly if not blocked (forced movement — no OAs, no Booming Blade). The push is conditional on the destination being unblocked AND the target being Medium-or-smaller (canon size constraint). Distinct from Thorn Whip / Lightning Lure (pull TOWARD) and Infestation (RANDOM direction).
- **Advantage-on-next-attack self-buff architecture (True Strike):** Set a scratch flag on the caster in `applySelfEffect` (CANTRIP_SELF_EFFECTS). In `resolveAttack`'s attack-roll branch, fold the flag into the `advantage` boolean (NOT the `disadvantage` boolean — distinct from Frostbite/Vicious Mockery). The `advantage` variable must be computed AFTER the flag is read (moved from line 239 to line 322 in Session 10). Consume (set to false) after the attack roll resolves — one-shot. Cleanup via `resetBudget`.
- **Save-bonus self-buff architecture (Resistance):** Set a scratch flag (value = die size, 4 = d4) on the caster in `applySelfEffect` (CANTRIP_SELF_EFFECTS). In `rollSave` (utils.ts), roll rollDie(value), ADD to the save total (mirror Mind Sliver's SUBTRACT, opposite sign), then consume (set to undefined). The save total formula became `roll + mod + prof + biBonus + blessBonus + wbBonus - mindSliverPenalty + resistanceBonus`. Cleanup via `resetBudget`.
- **Missing `rollAbilityCheck` choke point (forward-compat for Session 11):** The engine currently has NO `rollAbilityCheck(combatant, ability, dc)` function. Guidance (ability-check +1d4) and Friends (CHA-check advantage) will set scratch flags in v1 but have no choke point to consume them — documented via v1 simplification flags. Future work: add `rollAbilityCheck` to utils.ts (mirror `rollSave`'s architecture — fold in `_guidanceDieBonusNextAbilityCheck` and `_friendsAdvNextChaCheck`).

---

## AGENT PROTOCOL (PERPETUAL)

- **REPOSITORY:** https://github.com/mcabel/dnd-combat-sim
- **COMMIT POLICY:** Always `git add` and `git commit` the `zHANDOVER-SESSION-*.md` file to the project root directory. **MUST UPLOAD CODE/ARTIFACTS/ETC TO THE GITHUB REPO AFTER THE ZHANDOVER IS PRODUCED.** Tell the user explicitly if a push fails.
- **OUTPUT POLICY:**
  - PRIORITY: Upload/commit code/artifacts to the GitHub repo.
  - ONLY output the handover in the chat if access to the repo is somehow blocked.
  - NO summaries, no conversational filler, no "Here is the file" headers.
