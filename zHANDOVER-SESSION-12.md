# zHANDOVER-SESSION-12

## REPOSITORY

- Branch: main
- Prior commits (cantrip workstream):
  - `<new>` — Cantrip-11: Implement Spare the Dying, Guidance, Friends, Light, Minor Illusion (PHB)
  - `6a7704f` — Cantrip-10: Implement Gust, Primal Savagery, True Strike, Resistance, Magic Stone (XGE/PHB)
  - `abf347d` — Cantrip-9: Implement Infestation, Word of Radiance, Create Bonfire, Produce Flame, Shillelagh (XGE/PHB)
  - `fe8cec1` — Cantrip-8: Implement Frostbite, Green-Flame Blade, Lightning Lure, Sword Burst, Sapping Sting (XGE/TCE/EGW)
  - `c4cfc11` — Cantrip-7: Implement Eldritch Blast, Toll the Dead, Mind Sliver, Thunderclap, Booming Blade (PHB/XGE/TCE)
  - `c975049` — Cantrip-6: Implement Fire Bolt, Acid Splash, Poison Spray, Vicious Mockery, Sacred Flame (PHB)
  - `bc4d033` — Cantrip-3/4/5: Recover lost sessions (Chill Touch + Blade Ward + handovers)
  - `f2f40a3` — Spell-cache: per-level cache + batch picker tooling
- Commits this session:
  - `<new>` — Cantrip-11: Implement Spare the Dying, Guidance, Friends, Light, Minor Illusion (PHB)
- URL: https://github.com/mcabel/dnd-combat-sim
- PAT: user provides in chat each session. do not warn.

---

## ⚠️ WORKSTREAM OWNERSHIP — READ FIRST

| File family | Workstream | Owner |
|---|---|---|
| `zHANDOVER-SESSION-*.md` | **Cantrips** | **THIS agent (you)** |
| `HANDOVER-SESSION-*.md` | Core Engine / leveled spells | other agent — DO NOT TOUCH |
| `SHEET-HANDOVER-*.md` | Character Sheet UI | other agent — DO NOT TOUCH |

### Your priorities (cantrip workstream)

- **Your tasks come from `zHANDOVER-SESSION-*.md`.** This handover defines the next batch.
- Implement cantrips per PHB/XGE/TCE/EGW (2014 canon, pre-2024 only). Sessions 1–11 implemented 35 cantrips. This session extends to the **next batch of 5 cantrips** (curated for mechanical diversity — utility/transmutation batch, since the remaining 11 in-scope cantrips are all utility with no combat damage).
- Reuse the cantrip architecture in `src/engine/cantrip_effects.ts` — now **FIVE registries** (Session 11 added `CANTRIP_TOUCH_EFFECTS` for Spare the Dying + Light; added Guidance/Friends/Minor Illusion to `CANTRIP_SELF_EFFECTS`):
  - `CANTRIP_EFFECTS` (13) — post-hit / post-save-fail riders: Thorn Whip, Ray of Frost, Shocking Grasp, Chill Touch, Vicious Mockery, Mind Sliver, Booming Blade, Frostbite, Sapping Sting, Lightning Lure, Green-Flame Blade, Infestation, Gust
  - `CANTRIP_ATTACK_ADVANTAGE` (1) — pre-roll advantage: Shocking Grasp
  - `CANTRIP_SELF_EFFECTS` (7) — non-attack self-buffs: Blade Ward, Shillelagh, True Strike, Resistance, **Guidance**, **Friends**, **Minor Illusion**
  - `CANTRIP_AOE_EFFECTS` (3) — caster-centered AoE: Thunderclap, Sword Burst, Word of Radiance
  - `CANTRIP_TOUCH_EFFECTS` (2) — non-attack touch-effect on a single target (downed ally or willing creature): **Spare the Dying**, **Light**
  - (Primal Savagery + Magic Stone are metadata-only — no registry entry.)
- Do NOT create a `case 'spellName'` in `executePlannedAction` for cantrips.
- Do NOT touch sheet routes, `leveler.ts`, or `builder.ts` (TASK.md constraint).

---

## STARTUP CHECKLIST (do these before implementing)

1. `git pull` — make sure you're on the latest `main`.
2. Read **`zHANDOVER-SESSION-11.md`** (Session 11 patterns: Spare the Dying's NEW `CANTRIP_TOUCH_EFFECTS` registry + `resolveCantripTouchEffect(caster, target, actionName, state)` dispatcher consulted in `executePlannedAction` AFTER `resolveCantripAction`/`resolveCantripAoE` but BEFORE the target-null guard — CRITICAL: Spare the Dying's target is an UNCONSCIOUS ally at 0 HP, which the standard `if (!target || target.isDead || target.isUnconscious) break;` guard would BLOCK; the handler sets `target._isStabilized = true` + resets `target.deathSaves = {0,0}` (mirror `rollDeathSave`'s stable outcome) + fizzles on monsters/dead/above-0-HP targets; Guidance's `_guidanceDieBonusNextAbilityCheck?: number` scratch field mirroring Resistance's `_resistanceDieBonusNextSave` but for ABILITY CHECKS instead of SAVES — flag is set on cast but NOT consumed in v1 (the future `rollAbilityCheck` choke point doesn't exist yet); Friends's `_friendsAdvNextChaCheck?: boolean` scratch field mirroring True Strike's `_trueStrikeAdvNextAttack` but for CHA CHECKS instead of ATTACK ROLLS — same v1 non-consumption caveat; Light's `_lightSourceActive?: boolean` scratch field set on the TARGET (not caster) for forward-compat with the future vision subsystem — v1 sets the flag but `computeLOS` does NOT yet consume it; Minor Illusion's metadata-only flavor-log pattern via `CANTRIP_SELF_EFFECTS` — NO scratch fields, NO mechanical effect, just emits an "illusion created" log event). These are your templates.
3. Read `zHANDOVER-SESSION-10.md` (Gust's `pushAway(caster, target, state)` for push-AWAY forced movement; Primal Savagery's metadata-only melee spell attack pattern; True Strike's scratch-flag + advantage fold-in; Resistance's scratch-flag + `rollSave` integration; Magic Stone's metadata-only ranged spell attack with `damageBonusField: 'spellcastingMod'`). Also templates.
4. Read `zHANDOVER-SESSION-9.md` (Infestation's `rollRandomDirection()` + `isDestinationBlocked()` helpers, Word of Radiance's `CANTRIP_AOE_EFFECTS` registration, Shillelagh's `_shillelaghActive` scratch field, Produce Flame's metadata-only THROW-mode v1 simplification, Create Bonfire's on-cast-only v1 simplification). Also templates.
5. Read `zHANDOVER-SESSION-8.md` (Frostbite's weapon-attack-only filter, Sapping Sting's `addCondition` for prone, Lightning Lure's `action.damage = null` for conditional damage, Green-Flame Blade's `findSplashTarget` + `rollSplashDamage`, Sword Burst's `CANTRIP_AOE_EFFECTS`). Also templates.
6. Read `zHANDOVER-SESSION-7.md` (Mind Sliver's `rollSave` integration, Booming Blade's `executeMove` movement hook, Thunderclap's `CANTRIP_AOE_EFFECTS` registry, Toll the Dead's conditional damage, Eldritch Blast's multi-beam v1 simplification). Also templates.
7. Read `SPELL-CACHE.md` — it explains the cache + picker workflow.
8. Run `npm install` (deps: ts-node, typescript).
9. Run `npm run spell-cache:build` — refresh the cache (confirm 35/49 cantrips implemented).
10. For each cantrip below, run `npm run spell-cache:show -- "Name"` to get the full raw 5etools JSON (entries, scalingLevelDice, components) before implementing.

---

## GOALS THIS SESSION — Batch: 5 cantrips (utility / flavor / out-of-combat)

Picked from `spell-cache/level-0.json` (curated for mechanical diversity — the remaining 11 in-scope cantrips are all utility with no combat damage; this batch picks 5 that exercise the metadata-only flavor-log pattern introduced in Session 11 (Minor Illusion) and the CANTRIP_TOUCH_EFFECTS registry introduced in Session 11 (Spare the Dying + Light)). These 5 push the cantrip architecture into new territory: a persistent-spectral-hand utility (Mage Hand), a long-duration multi-effect sensory spell (Prestidigitation), a divine-themed sensory spell (Thaumaturgy), an out-of-combat object-repair spell with a 1-MINUTE casting time (Mending — the first cantrip with a non-action casting time), and a ranged point-to-point communication spell (Message — forward-compat for a "communication" subsystem).

| # | Name | School | Effect | Source | Page | Module to create |
|---|------|--------|--------|--------|------|------------------|
| 1 | **Mage Hand** | Conjuration | 30 ft · 1 min · **spectral floating hand appears at a point within range; can manipulate objects, open unlocked doors, stow/retrieve items, pour vials; can't attack, activate magic items, or carry >10 lbs** · v1 simplification: metadata-only flavor log via `CANTRIP_SELF_EFFECTS` (mirror Minor Illusion); skip the persistent-hand subsystem (the engine does not yet model remote object manipulation — forward-compat TODO via `mageHandRemoteInteractionV1Implemented: false`) | PHB (2014-08-19) | 256 | `src/spells/mage_hand.ts` |
| 2 | **Prestidigitation** | Transmutation | 10 ft · up to 1 hour · **create one of 6 magical effects: harmless sensory effect, light/snuff candle/torch/campfire, clean/soil 1-ft³ object, chill/warm/flavor 1-ft³ nonliving material, color/mark/symbol on object, nonmagical trinket or illusory image in hand** · can have up to 3 non-instantaneous effects active · v1 simplification: metadata-only flavor log via `CANTRIP_SELF_EFFECTS` (mirror Minor Illusion); skip the multi-effect tracking subsystem (the engine does not yet model persistent sensory effects — forward-compat TODO via `prestidigitationMultiEffectTrackingV1Implemented: false`) | PHB (2014-08-19) | 267 | `src/spells/prestidigitation.ts` |
| 3 | **Thaumaturgy** | Transmutation | 30 ft · up to 1 min · **manifest one of 6 magical effects: voice booms 3× louder, flames flicker/brighten/dim/color, harmless tremors, instantaneous sound, unlocked door/window open/shut, alter eye appearance** · can have up to 3 1-minute effects active · v1 simplification: metadata-only flavor log via `CANTRIP_SELF_EFFECTS` (mirror Prestidigitation but divine-themed); skip the multi-effect tracking subsystem (forward-compat TODO via `thaumaturgyMultiEffectTrackingV1Implemented: false`) | PHB (2014-08-19) | 282 | `src/spells/thaumaturgy.ts` |
| 4 | **Mending** | Transmutation | touch · instant · **repair a single break or tear in an object no larger than 1 foot in any dimension (broken chain link, broken key halves, torn cloak, leaking wineskin)** · can physically repair a magic item or construct but can't restore magic · **CANON CASTING TIME: 1 MINUTE (NOT 1 action)** — this is the FIRST cantrip with a non-action casting time (1 min = 10 rounds = out-of-combat only) · v1 simplification: metadata-only via `CANTRIP_TOUCH_EFFECTS` (mirror Light v1 — set a `_mended?: boolean` scratch flag on the target for forward-compat); skip the casting-time enforcement (v1 treats as a standard action for engine simplicity — documented via `mendingCastingTimeV1Simplified: true`); skip the construct-repair subsystem (forward-compat TODO via `mendingConstructRepairV1Implemented: false`) | PHB (2014-08-19) | 259 | `src/spells/mending.ts` |
| 5 | **Message** | Transmutation | 120 ft · 1 round · **point at a creature within range and whisper a message; target (and only target) hears the message and can reply in a whisper only you hear** · can cast through solid objects if familiar with target; magical silence, 1 ft stone, 1 inch metal, thin lead sheet, or 3 ft wood blocks the spell · v1 simplification: metadata-only flavor log via `CANTRIP_SELF_EFFECTS` (mirror Minor Illusion); skip the communication subsystem (the engine does not yet model point-to-point messaging — forward-compat TODO via `messageCommunicationV1Implemented: false`) | PHB (2014-08-19) | 259 | `src/spells/message.ts` |

### Implementation checklist (paste into the session's completion notes)

- [ ] **Mage Hand** (`PHB p.256`) — 30 ft range, 1 min duration, V+S (NO M — canon 5etools JSON: `{"v":true,"s":true}`). **Effect: spectral floating hand appears; can manipulate objects, open doors, stow/retrieve items, pour vials; can't attack, activate magic items, or carry >10 lbs.** v1 simplification: metadata-only flavor log via `CANTRIP_SELF_EFFECTS` (mirror Minor Illusion). NO scratch fields (v1 has no persistent state — the spectral hand is flavor-only). NO new Combatant fields. v1 simplification flags: `mageHandRemoteInteractionV1Implemented: false` (engine lacks a remote-interaction subsystem) AND `mageHandPersistentHandV1Simplified: true` (canon: 1 min duration with per-action control; v1: instant flavor log) AND `mageHandDismissalV1Implemented: false` (canon: "dismiss it as an action"; v1: no dismissal) AND `mageHandCarryWeightLimitV1Implemented: false` (canon: 10 lb carry limit; v1: no carry modeling). Components: V+S (no M). NO CANTRIP_EFFECTS entry (not a post-hit rider). NO CANTRIP_TOUCH_EFFECTS entry (not a touch cantrip — 30 ft range). Register in `CANTRIP_SELF_EFFECTS` (mirror Minor Illusion — flavor-only self-buff that emits a single log event). applySelfEffect emits "casts Mage Hand — a spectral floating hand appears" log + returns true. cleanup is a no-op.
- [ ] **Prestidigitation** (`PHB p.267`) — 10 ft range, up to 1 hour duration, V+S (NO M — canon 5etools JSON: `{"v":true,"s":true}`). **Effect: create one of 6 magical effects (sensory, light/snuff, clean/soil, chill/warm/flavor, color/mark, trinket/illusory image).** Canon: up to 3 non-instantaneous effects active simultaneously. v1 simplification: metadata-only flavor log via `CANTRIP_SELF_EFFECTS` (mirror Minor Illusion). NO scratch fields. NO new Combatant fields. v1 simplification flags: `prestidigitationMultiEffectTrackingV1Implemented: false` (canon: up to 3 active; v1: single flavor log) AND `prestidigitationEffectChoiceV1Simplified: true` (canon: choose 1 of 6; v1: emit single "creates a minor magical effect" log without choosing) AND `prestidigitationDurationV1Simplified: true` (canon: up to 1 hour; v1: 1-round flavor log) AND `prestidigitationDismissalV1Implemented: false` (canon: dismiss as action; v1: no dismissal). Components: V+S (no M). NO CANTRIP_EFFECTS entry. NO CANTRIP_TOUCH_EFFECTS entry. Register in `CANTRIP_SELF_EFFECTS`. applySelfEffect emits "casts Prestidigitation — creates a minor magical effect (sensory, light, clean, flavor, color, or trinket)" log + returns true. cleanup is a no-op.
- [ ] **Thaumaturgy** (`PHB p.282`) — 30 ft range, up to 1 min duration, **V only (NO S, NO M — canon 5etools JSON: `{"v":true}`)**. **Effect: manifest one of 6 magical effects (booming voice, flame flicker, tremors, sound, door/window open/shut, eye appearance).** Canon: up to 3 1-minute effects active simultaneously. v1 simplification: metadata-only flavor log via `CANTRIP_SELF_EFFECTS` (mirror Prestidigitation but divine-themed). NO scratch fields. NO new Combatant fields. v1 simplification flags: `thaumaturgyMultiEffectTrackingV1Implemented: false` AND `thaumaturgyEffectChoiceV1Simplified: true` AND `thaumaturgyDurationV1Simplified: true` AND `thaumaturgyDismissalV1Implemented: false`. Components: V only (no S, no M). Register in `CANTRIP_SELF_EFFECTS`. applySelfEffect emits "casts Thaumaturgy — manifests a minor supernatural sign (booming voice, flame, tremors, sound, door, or eyes)" log + returns true. cleanup is a no-op.
- [ ] **Mending** (`PHB p.259`) — touch range, instant, V+S+M (two lodestones — canon 5etools JSON: `{"v":true,"s":true,"m":"two lodestones"}`). **CANON CASTING TIME: 1 MINUTE (NOT 1 action).** This is the FIRST cantrip with a non-action casting time (1 min = 10 rounds = out-of-combat only per PHB p.192 casting-time rules). **Effect: repair a single break or tear in an object no larger than 1 foot in any dimension.** Can physically repair a magic item or construct but can't restore magic. v1 simplification: metadata-only via `CANTRIP_TOUCH_EFFECTS` (mirror Light v1 — set a `_mended?: boolean` scratch flag on the target for forward-compat with a future object-state subsystem). NEW scratch field on `Combatant`: `_mended?: boolean` (forward-compat — the future object-state subsystem will read it; v1 sets the flag but nothing consumes it). v1 simplification flags: `mendingCastingTimeV1Simplified: true` (canon: 1 min; v1: standard action for engine simplicity) AND `mendingConstructRepairV1Implemented: false` (canon: can physically repair constructs; v1: no construct-heal subsystem) AND `mendingMagicItemRestorationV1Implemented: false` (canon: can physically repair magic items but can't restore magic; v1: no magic-item-state subsystem) AND `mendingBreakSizeLimitV1Simplified: true` (canon: break/tear no larger than 1 foot; v1: no size check). Components: V+S+M (two lodestones). Register in `CANTRIP_TOUCH_EFFECTS` (mirror Light v1 — touch-effect on a single target, sets a forward-compat flag). applyTouchEffect sets `target._mended = true` + emits "casts Mending — repairs a break or tear in {target.name}" log + returns true. Fizzle conditions: target is dead (defensive). cleanup clears the `_mended` flag (defensive — the flag is forward-compat only).
- [ ] **Message** (`PHB p.259`) — 120 ft range, 1 round duration, V+S+M (a short piece of copper wire — canon 5etools JSON: `{"v":true,"s":true,"m":"a short piece of copper wire"}`). **Effect: point at a creature within range and whisper a message; target hears and can reply.** Can cast through solid objects if familiar with target; magical silence, 1 ft stone, 1 inch metal, thin lead sheet, or 3 ft wood blocks the spell. v1 simplification: metadata-only flavor log via `CANTRIP_SELF_EFFECTS` (mirror Minor Illusion). NO scratch fields. NO new Combatant fields. v1 simplification flags: `messageCommunicationV1Implemented: false` (engine lacks a point-to-point communication subsystem) AND `messageBarrierBlockingV1Simplified: true` (canon: blocked by silence/stone/metal/lead/wood; v1: no barrier check) AND `messageFamiliarityRequirementV1Simplified: true` (canon: cast through solid objects only if familiar with target; v1: no familiarity tracking) AND `messageReplyMechanicV1Implemented: false` (canon: target can reply in a whisper; v1: no reply action). Components: V+S+M (copper wire). NO CANTRIP_EFFECTS entry. NO CANTRIP_TOUCH_EFFECTS entry. Register in `CANTRIP_SELF_EFFECTS`. applySelfEffect emits "casts Message — whispers a message to a creature within range" log + returns true. cleanup is a no-op.

### Integration points you will touch (expected)

- `src/types/core.ts`: Add `_mended?: boolean` to `Combatant` (Mending — forward-compat for the future object-state subsystem; v1 sets the flag but nothing consumes it). Mage Hand, Prestidigitation, Thaumaturgy, Message need NO new scratch fields (v1 is metadata-only with no persistent state, mirror Minor Illusion).
- `src/engine/cantrip_effects.ts`: Add `'Mage Hand'`, `'Prestidigitation'`, `'Thaumaturgy'`, `'Message'` to `CANTRIP_SELF_EFFECTS` (mirror Minor Illusion — flavor-only self-buffs that emit a single log event). Add `'Mending'` to `CANTRIP_TOUCH_EFFECTS` (mirror Light v1 — touch-effect on a single target, sets a forward-compat flag). Update module header to list all 26 supported cantrips across 5 registries.
- `src/engine/combat.ts`:
  - `executePlannedAction` `case 'cast':` — NO changes needed (Mage Hand / Prestidigitation / Thaumaturgy / Message auto-route via `resolveCantripAction` for self-buffs; Mending auto-routes via `resolveCantripTouchEffect` for touch-effects — both already consulted in Session 11).
  - `resolveAttack` attack branch — NO changes needed (none of these 5 cantrips have an attack roll).
  - `executeMove` — NO changes needed.
- `src/engine/utils.ts`:
  - `resetBudget` — add `cleanupMending(c)` (clears the `_mended` flag if not consumed before the start of the caster's next turn — v1 simplification, defensive cleanup). Mage Hand, Prestidigitation, Thaumaturgy, Message need NO cleanup (v1 has no persistent state, mirror Minor Illusion's no-op cleanup).
  - `rollSave` — NO changes needed.
  - **FUTURE WORK (out of scope for this batch):** add a `rollAbilityCheck(combatant, ability, dc)` function (mirror `rollSave`'s architecture — fold in `_guidanceDieBonusNextAbilityCheck` and `_friendsAdvNextChaCheck` from Session 11). Still missing — Session 11 documented this as forward-compat.
- **Do NOT** touch the AI planner (`src/ai/planner.ts`) — that's Core Engine territory.

### Tests (write one `*.test.ts` per cantrip, in the repo's ts-node convention)

Mirror `src/test/spare_the_dying.test.ts` (71 tests — touch-effect + stabilize + fizzle conditions + CANTRIP_TOUCH_EFFECTS routing + bypasses resolveAttack), `src/test/guidance.test.ts` (52 tests — self-buff + scratch flag + cleanup + mirror Resistance), `src/test/friends.test.ts` (53 tests — self-buff + scratch flag + cleanup + mirror True Strike), `src/test/light.test.ts` (60 tests — touch-effect + forward-compat flag + cleanup + bypasses resolveAttack), and `src/test/minor_illusion.test.ts` (55 tests — metadata-only flavor log + no scratch fields + no mechanical effect) for structure. Use deterministic save outcomes (DC=30 for guaranteed fail, DC=1 + ability=30 for guaranteed success) and `isCritOverride=true` for forced attack-roll hits. Target ~50–70 tests per cantrip (fewer for pure flavor-only cantrips). Each test file must exit non-zero on failure (`process.exit(1)`).

**Critical test cases per cantrip:**
- Mage Hand: metadata; components (V+S — NO M); rangeFt = 30 (canon); isSelfBuff = true; v1 simplification flags (remote-interaction TODO + persistent-hand simplified + dismissal TODO + carry-weight TODO); does NOT scale; **applySelfEffect emits a single "Mage Hand" flavor log + returns true**; **NO scratch fields set on caster** (snapshot keys before/after — unchanged); **NO attack/damage/save events** (self-buff bypasses resolveAttack); resolveCantripAction integration; dispatcher safety; resolveCantripAoE returns false; resolveCantripTouchEffect returns false; no CANTRIP_EFFECTS entry; cleanup is a no-op; resetBudget integration (no-op); flavor log mentions "spectral" + "hand".
- Prestidigitation: metadata; components (V+S — NO M); rangeFt = 10 (canon); isSelfBuff = true; v1 simplification flags (multi-effect TODO + effect-choice simplified + duration simplified + dismissal TODO); does NOT scale; **applySelfEffect emits a single flavor log + returns true**; **NO scratch fields set**; **NO attack/damage/save events**; resolveCantripAction integration; dispatcher safety; resolveCantripAoE returns false; resolveCantripTouchEffect returns false; no CANTRIP_EFFECTS entry; cleanup is a no-op; flavor log mentions "minor magical effect" + at least one of (sensory/light/clean/flavor/color/trinket).
- Thaumaturgy: metadata; components (**V only — NO S, NO M**); rangeFt = 30 (canon); isSelfBuff = true; v1 simplification flags (multi-effect TODO + effect-choice simplified + duration simplified + dismissal TODO); does NOT scale; **applySelfEffect emits a single flavor log + returns true**; **NO scratch fields set**; **NO attack/damage/save events**; resolveCantripAction integration; dispatcher safety; resolveCantripAoE returns false; resolveCantripTouchEffect returns false; no CANTRIP_EFFECTS entry; cleanup is a no-op; flavor log mentions "supernatural sign" or "manifest" + at least one of (voice/flame/tremor/sound/door/eyes).
- Mending: metadata; components (V+S+M — two lodestones); rangeFt = 0 (Touch); isTouchEffect = true; v1 simplification flags (casting-time simplified + construct-repair TODO + magic-item-restoration TODO + break-size-limit simplified); does NOT scale; **applyTouchEffect sets `target._mended = true` + emits log + returns true**; **fizzle on dead creature** (defensive); **CANTRIP_TOUCH_EFFECTS routing** (verify the cantrip bypasses resolveAttack — no attack hit/miss/damage events); **DEX save NOT triggered** (Mending has no save); **1-minute casting time documented via flag** (canon: 1 min = out-of-combat; v1: standard action for engine simplicity); resolveCantripAction returns false (NOT a self-buff); resolveCantripAoE returns false; dispatcher safety; **cleanup clears `_mended` flag** (resetBudget integration); **Mending itself does NOT go through resolveAttack** (touch-effect); flavor log mentions "repair" + "break" or "tear".
- Message: metadata; components (V+S+M — copper wire); rangeFt = 120 (canon); isSelfBuff = true; v1 simplification flags (communication TODO + barrier-blocking simplified + familiarity-requirement simplified + reply-mechanic TODO); does NOT scale; **applySelfEffect emits a single flavor log + returns true**; **NO scratch fields set**; **NO attack/damage/save events**; resolveCantripAction integration; dispatcher safety; resolveCantripAoE returns false; resolveCantripTouchEffect returns false; no CANTRIP_EFFECTS entry; cleanup is a no-op; flavor log mentions "whisper" + "message".

---

## COMPLETED THIS SESSION (Session 11, for reference)

### Feature: 5 cantrips implemented

1. **Spare the Dying** (`src/spells/spare_the_dying.ts`) — PHB p.277. Touch range, instant, V+S (NO M — canon 5etools JSON: `{"v":true,"s":true}`). **Effect: stabilize a living creature at 0 HP.** This is the FIRST heal-adjacent cantrip — mirrors Healing Word's stabilize logic but as a CANTRIP (no slot cost) and touch range (vs Healing Word's 60 ft ranged). v1 simplification: implement only the stabilize effect (set `target._isStabilized = true` and reset `target.deathSaves = {successes: 0, failures: 0}` — mirror `rollDeathSave`'s "stable" outcome in utils.ts); skip the undead/construct exclusion (canon: "This spell has no effect on undead or constructs." — forward-compat TODO via `spareTheDyingTypeExclusionV1Implemented: false`). Components: V+S (no M). NO CANTRIP_EFFECTS entry (not a post-hit rider). NO CANTRIP_SELF_EFFECTS entry (the target is NOT the caster). **NEW ROUTING:** Spare the Dying is a non-attack, non-AoE, non-self-buff cantrip that targets a DOWNED ALLY — this is a NEW pattern. Route via a NEW registry `CANTRIP_TOUCH_EFFECTS` in `cantrip_effects.ts` (consulted by `executePlannedAction` AFTER `resolveCantripAction` and `resolveCantripAoE` but BEFORE the target-null guard — CRITICAL: Spare the Dying's target is UNCONSCIOUS, which the standard `if (!target || target.isDead || target.isUnconscious) break;` guard would BLOCK; the touch-effect handler receives `(caster, target, state)` and returns true if the touch effect was applied or fizzled — either way, the action is consumed). The handler checks `target.isDead` (fizzle), `!target.isPlayer && target.currentHP === 0` (fizzle — monsters die at 0 HP per PHB p.197), `target.currentHP > 0` (fizzle — only downed creatures can be stabilized); otherwise sets `target._isStabilized = true`, resets `target.deathSaves = {0, 0}`, emits a "stabilized" log. Target STAYS at 0 HP and STAYS unconscious (PHB p.197: stable creatures remain unconscious until healed). No scratch fields on the CASTER (the stabilize effect is instant and modifies TARGET state directly). No cleanup needed on the caster (the target's stabilized state persists until healed — there is no 1-round expiration).
2. **Guidance** (`src/spells/guidance.ts`) — PHB p.248. Touch range, 1 min concentration (v1: 1-round non-concentration), V+S (NO M — canon 5etools JSON: `{"v":true,"s":true}`). **Effect: target adds 1d4 to one ability check of its choice before the spell ends.** This is the FIRST ability-check-bonus cantrip. MIRRORS Resistance's `_resistanceDieBonusNextSave` scratch field but for ABILITY CHECKS instead of SAVES. NEW scratch field on `Combatant`: `_guidanceDieBonusNextAbilityCheck?: number` (set to 4 on cast — d4; consumed by the FUTURE `rollAbilityCheck` choke point in utils.ts — NOT YET IMPLEMENTED, documented via `guidanceAbilityCheckIntegrationV1Implemented: false`). v1 sets the flag on cast but does NOT consume it (no `rollAbilityCheck` choke point exists yet); the flag is cleared at the start of the caster's NEXT turn via `cleanup()` called from `resetBudget` (v1 1-round simplification). Components: V+S (no M). v1 simplification flags: `guidanceConcentrationV1Simplified: true` (canon: 1 min conc; v1: 1 round) AND `guidanceTouchAllyV1Simplified: true` (canon: any willing creature; v1: self-only) AND `guidanceAbilityCheckIntegrationV1Implemented: false`. Register in `CANTRIP_SELF_EFFECTS` (mirror Resistance).
3. **Friends** (`src/spells/friends.ts`) — PHB p.244. Self range, 1 min concentration (v1: 1-round non-concentration), **S+M (a small amount of makeup applied to the face as this spell is cast — canon 5etools JSON: `{"s":true,"m":"a small amount of makeup applied to the face as this spell is cast"}`, NO V)**. **Effect: advantage on all CHA checks directed at one creature of your choice that isn't hostile toward you.** This is the SECOND advantage-granting self-buff cantrip (after True Strike). MIRRORS True Strike's `_trueStrikeAdvNextAttack` scratch field but for CHA CHECKS instead of ATTACK ROLLS. NEW scratch field on `Combatant`: `_friendsAdvNextChaCheck?: boolean` (set on cast, consumed by the FUTURE `rollAbilityCheck` choke point — NOT YET IMPLEMENTED, documented via `friendsAbilityCheckIntegrationV1Implemented: false`). v1 sets the flag on cast but does NOT consume it; the flag is cleared at the start of the caster's NEXT turn via `cleanup()` (v1 1-round simplification). Components: S+M (makeup, no V). v1 simplification flags: `friendsConcentrationV1Simplified: true` AND `friendsTargetAgnosticV1Simplified: true` AND `friendsHostilityBacklashV1Implemented: false` (canon: "the creature realizes that you used magic to influence its mood and becomes hostile toward you" — v1 skips this backlash) AND `friendsAbilityCheckIntegrationV1Implemented: false`. Register in `CANTRIP_SELF_EFFECTS` (mirror True Strike).
4. **Light** (`src/spells/light.ts`) — PHB p.255. Touch range, 1 hour, **V+M (a firefly or phosphorescent moss — canon 5etools JSON: `{"v":true,"m":"a firefly or phosphorescent moss"}`, NO S)**. **Effect: object sheds bright light in 20-ft radius + dim light for additional 20 ft.** v1 simplification: implement only the on-cast effect (set a `_lightSourceActive?: boolean` scratch field on the TARGET — forward-compat for the vision subsystem). Skip the persistent-light-radius integration (the engine's `computeLOS` does not yet model light-radius-based vision changes — Darkness and similar effects also lack this integration; forward-compat TODO via `lightVisionIntegrationV1Implemented: false`). The hostile-creature DEX save IS documented as a v1 simplification (canon: "If you target an object held or worn by a hostile creature, that creature must succeed on a Dexterity saving throw to avoid the spell." — v1 treats ALL targets as "willing" via `lightHostileTargetSaveV1Simplified: true`). Components: V+M (firefly/phosphorescent moss, NO S). v1 simplification flags: `lightVisionIntegrationV1Implemented: false` AND `lightDismissalV1Implemented: false` (canon: "dismiss it as an action" — v1 has no dismissal action) AND `lightRecastEndsPreviousV1Implemented: false` (canon: "The spell ends if you cast it again" — v1 has no recast-tracking) AND `lightHostileTargetSaveV1Simplified: true` AND `lightDurationV1Simplified: true` (canon: 1 hour; v1: 1 round) AND `lightRangeEnforcementV1Simplified: true`. NO CANTRIP_EFFECTS entry (Light is not a post-hit rider). NO CANTRIP_SELF_EFFECTS entry (Light targets an OBJECT, not the caster). NO CANTRIP_AOE_EFFECTS entry (Light is not a caster-centered AoE). **ROUTING:** Route via `CANTRIP_TOUCH_EFFECTS` (the NEW registry introduced in this session — mirror Spare the Dying's routing). applyTouchEffect sets `target._lightSourceActive = true` + emits "begins to glow with light" log + returns true. Fizzle on dead target (defensive). cleanup clears the `_lightSourceActive` flag (defensive — the flag is forward-compat only).
5. **Minor Illusion** (`src/spells/minor_illusion.ts`) — PHB p.260. 30 ft range, 1 min, **S+M (a bit of fleece — canon 5etools JSON: `{"s":true,"m":"a bit of fleece"}`, NO V)**. **Effect: create a sound or an image of an object within range that lasts for the duration.** v1 simplification: implement only the on-cast effect (emit a "creates an illusion" log; NO scratch fields, NO persistent-illusion tracking). Skip the INT (Investigation) check to disbelieve (the engine has no `rollAbilityCheck` choke point yet — same blocker as Guidance/Friends). Skip the illusion-as-cover integration (the engine's `computeLOS` does not yet model illusions-as-cover — forward-compat TODO via `illusionMechanicsV1Implemented: false`). Components: S+M (bit of fleece, no V). v1 simplification flags: `illusionMechanicsV1Implemented: false` (no Investigation check, no cover integration, no physical-interaction reveal) AND `illusionSoundVsImageV1Simplified: true` (canon: choose sound OR image; v1: emit a single "illusion" log without distinguishing) AND `illusionDurationV1Simplified: true` (canon: 1 min; v1: 1 round) AND `illusionRangeEnforcementV1Simplified: true`. Register in `CANTRIP_SELF_EFFECTS` (v1 treats it as a flavor-only self-buff that emits a single log event — no target required, no mechanical effect). applySelfEffect emits "casts Minor Illusion — creates a sound or an image of an object" log + returns true. cleanup is a no-op (no scratch fields).

### Integration points touched (Session 11)

- `src/types/core.ts`: Added `_guidanceDieBonusNextAbilityCheck?: number` to `Combatant` (Guidance). Added `_friendsAdvNextChaCheck?: boolean` to `Combatant` (Friends). Added `_lightSourceActive?: boolean` to `Combatant` (Light — forward-compat for the vision subsystem). Added `_isStabilized?: boolean` to `Combatant` (Spare the Dying — flag for the death-saves subsystem to read). Minor Illusion needs NO new scratch fields (v1 is metadata-only with no persistent state).
- `src/engine/cantrip_effects.ts`: Added a NEW registry `CANTRIP_TOUCH_EFFECTS` (handler signature `(caster, target, state) => boolean` — mirror `CANTRIP_EFFECTS` but called BEFORE resolveAttack, like `CANTRIP_SELF_EFFECTS`). Registered `'Spare the Dying'` and `'Light'` in `CANTRIP_TOUCH_EFFECTS`. Registered `'Guidance'`, `'Friends'`, `'Minor Illusion'` in `CANTRIP_SELF_EFFECTS`. Added a NEW dispatcher function `resolveCantripTouchEffect(caster, target, actionName, state) => boolean` (returns true if the cantrip name is registered — the action is consumed and resolveAttack is bypassed; if target is null, the spell fizzles but the action is still consumed). Updated module header to list all 26 supported cantrips across 5 registries.
- `src/engine/combat.ts`:
  - **`executePlannedAction` `case 'cast':`** — added a NEW consultation of `resolveCantripTouchEffect(actor, target ?? null, plan.action.name, state)` AFTER `resolveCantripAction` (self-buffs) and `resolveCantripAoE` (AoE), but BEFORE the target-null guard. CRITICAL: this routing must come BEFORE `if (!target || target.isDead || target.isUnconscious) break;` because Spare the Dying's target is UNCONSCIOUS (the standard guard would BLOCK unconscious targets). The touch-effect handler itself decides whether the spell fires (Spare the Dying fizzles on monsters at 0 HP, on creatures above 0 HP, and on dead creatures).
  - **`resolveAttack` attack branch** — NO changes needed (Guidance and Friends are for ability checks, not attack rolls; True Strike already covers attack advantage; Light has no attack roll; Minor Illusion has no attack roll; Spare the Dying has no attack roll).
  - **`executeMove`** — NO changes needed.
- `src/engine/utils.ts`:
  - `resetBudget` now calls `cleanupGuidance(c)`, `cleanupFriends(c)`, `cleanupLight(c)` (clear the flags if not consumed before the start of the caster's next turn — v1 simplification). Spare the Dying and Minor Illusion need NO cleanup (Spare the Dying's stabilize is instant and persists on the TARGET; Minor Illusion v1 has no persistent state). `cleanupLight` defensively clears the `_lightSourceActive` flag from ANY combatant that has it set (the flag is normally on the TARGET, but v1's cleanup operates on the combatant whose turn is starting — the caster; defensive cleanup ensures no stale flags persist).
  - `rollSave` — NO changes needed (Guidance and Friends are for ability checks, not saves — Resistance already covers the save-bonus integration).
  - **FUTURE WORK (out of scope for this batch):** add a `rollAbilityCheck(combatant, ability, dc)` function (mirror `rollSave`'s architecture — fold in `_guidanceDieBonusNextAbilityCheck` and `_friendsAdvNextChaCheck`). The engine currently has NO `rollAbilityCheck` choke point; v1 sets the flags but does not consume them (documented via the v1 simplification flags).

### Tests (Session 11)

- `src/test/spare_the_dying.test.ts`: 71 tests. Metadata, school (necromancy), components (V+S — NO M, canon 5etools JSON), rangeFt = 0 (Touch), isTouchEffect = true, v1 simplification flags (type-exclusion TODO + range-enforcement simplified), does NOT scale (stabilize is binary), **applyTouchEffect stabilizes downed PC ally** (sets `_isStabilized = true` + resets `deathSaves = {0, 0}` + target STAYS at 0 HP and unconscious — mirror `rollDeathSave`'s stable outcome), **monster at 0 HP → fizzle** (defensive branch + main branch via `isDead`), **creature above 0 HP → fizzle** (log mentions "not at 0 HP"), **dead creature → fizzle** (log mentions "dead"), **CANTRIP_TOUCH_EFFECTS routing** (verify the cantrip bypasses resolveAttack — no attack/damage/save events), **DEX save NOT triggered** (Spare the Dying has no save — touch range, willing target), **stabilize persists on TARGET** (no 1-round expiration — survives `resetBudget` on caster AND target), **canon text documented** (undead/construct exclusion TODO via flag — v1 DOES stabilize undead ally, TODO acknowledged), resolveCantripAction returns false (NOT a self-buff), resolveCantripAoE returns false (NOT a caster-centered AoE), no CANTRIP_EFFECTS entry (NOT a post-hit rider), resolveCantripTouchEffect returns true when target is null (fizzle, action consumed), dispatcher safety, cleanup is a no-op on the caster.
- `src/test/guidance.test.ts`: 52 tests. Metadata, school (divination), components (V+S — NO M, canon 5etools JSON), rangeFt = 0 (Touch), isSelfBuff = true, riderDieSides = 4 (d4), v1 simplification flags (concentration + touch-ally + ability-check-integration TODO), does NOT scale, **applySelfEffect sets `_guidanceDieBonusNextAbilityCheck = 4` + emits log** (log mentions "+1d4" + "ability check"), resolveCantripAction integration, dispatcher safety, **cleanup clears flag** (resetBudget integration), **v1 does NOT consume the flag** (no rollAbilityCheck choke point — flag persists until cleanup), **buff clears at start of next turn** (resetBudget), **Guidance bypasses resolveAttack** (self-buff), resolveCantripAoE returns false, resolveCantripTouchEffect returns false, no CANTRIP_EFFECTS entry, **mirror Resistance architecture** (same die size 4 = d4, same one-shot semantics, same isSelfBuff, same scales — only the choke point differs: rollSave for Resistance vs future rollAbilityCheck for Guidance).
- `src/test/friends.test.ts`: 53 tests. Metadata, school (enchantment), components (**S+M — makeup, NO V** — canon 5etools JSON), rangeFt = 0 (Self), isSelfBuff = true, riderCheckAbility = 'cha', v1 simplification flags (concentration + target-agnostic + hostility-backlash TODO + CHA-check-integration TODO), does NOT scale, **applySelfEffect sets `_friendsAdvNextChaCheck = true` + emits log** (log mentions "advantage" + "Charisma"/"CHA"), resolveCantripAction integration, dispatcher safety, **cleanup clears flag** (resetBudget integration), **v1 does NOT consume the flag**, **buff clears at start of next turn**, **Friends bypasses resolveAttack** (self-buff), resolveCantripAoE returns false, resolveCantripTouchEffect returns false, no CANTRIP_EFFECTS entry, **mirror True Strike architecture** (same boolean flag, same one-shot semantics, same isSelfBuff, same scales, same concentration=false — only the choke point differs: resolveAttack for True Strike vs future rollAbilityCheck for Friends).
- `src/test/light.test.ts`: 60 tests. Metadata, school (evocation), components (**V+M — firefly/phosphorescent moss, NO S** — canon 5etools JSON), rangeFt = 0 (Touch), isTouchEffect = true, brightLightRadiusFt = 20 (canon), dimLightRadiusFt = 20 (canon), v1 simplification flags (vision-integration TODO + dismissal TODO + recast-ends-previous TODO + hostile-target-save simplified + duration simplified + range-enforcement simplified), does NOT scale, **applyTouchEffect sets `_lightSourceActive = true` on TARGET (not caster) + emits log** (log mentions "glow"/"light" + "20"), **dead creature → fizzle** (log mentions "no effect"), **CANTRIP_TOUCH_EFFECTS routing** (bypasses resolveAttack — no attack/damage/save events), **DEX save NOT triggered in v1** (all targets treated as willing — even monster target IS ignited, no save log), **v1 flag is forward-compat only** (lightVisionIntegrationV1Implemented = false acknowledged), resolveCantripAction returns false (NOT a self-buff), resolveCantripAoE returns false, no CANTRIP_EFFECTS entry, resolveCantripTouchEffect returns true when target is null (fizzle, action consumed), dispatcher safety, **cleanup clears `_lightSourceActive` flag** (resetBudget integration — self-cast case), flavor log mentions caster name + target name + "casts Light" + "20" + "glow"/"light".
- `src/test/minor_illusion.test.ts`: 55 tests. Metadata, school (illusion), components (**S+M — bit of fleece, NO V** — canon 5etools JSON), rangeFt = 30 (canon), isSelfBuff = true, v1 simplification flags (sound-vs-image simplified + mechanics TODO + duration simplified + range-enforcement simplified), does NOT scale, **applySelfEffect emits a single flavor log + returns true** (exactly 1 action event), **NO scratch fields set on caster** (snapshot keys before/after — unchanged; specifically verifies `_guidanceDieBonusNextAbilityCheck`, `_friendsAdvNextChaCheck`, `_lightSourceActive`, `_isStabilized`, `_trueStrikeAdvNextAttack`, `_resistanceDieBonusNextSave` all undefined), **NO attack/damage/save events**, resolveCantripAction integration, dispatcher safety, resolveCantripAoE returns false, resolveCantripTouchEffect returns false, no CANTRIP_EFFECTS entry, **cleanup is a no-op** (caster state unchanged), resetBudget integration (no-op), **Minor Illusion bypasses resolveAttack** (self-buff), **NO new Combatant fields** (v1 has no scratch fields — no `_minorIllusionActive` field), flavor log mentions "sound"/"image" + "illusion", **v1 simplification flags document missing mechanics** (no save_success/save_fail events, no "DC" mention — Investigation check NOT triggered despite high-INT examiner present).

**Total new tests: 291.** All use deterministic save outcomes (DC=30 / DC=1+ability=30) and `isCritOverride=true` for forced attack hits (avoids the 5% nat-1 flakiness documented in prior handovers).

---

## DISCOVERIES / PATTERNS FROM THIS SESSION (reuse these)

1. **Five cantrip registries now well-populated.** 13 cantrips in `CANTRIP_EFFECTS`, 1 in `CANTRIP_ATTACK_ADVANTAGE`, 7 in `CANTRIP_SELF_EFFECTS` (added Guidance + Friends + Minor Illusion), 3 in `CANTRIP_AOE_EFFECTS`, 2 in `CANTRIP_TOUCH_EFFECTS` (NEW — added Spare the Dying + Light). The architecture is flexible enough for any cantrip pattern. The Session 12 batch will add 4 more self-buffs (Mage Hand, Prestidigitation, Thaumaturgy, Message — all metadata-only flavor logs mirroring Minor Illusion) and 1 more touch-effect (Mending — mirror Light v1, sets a forward-compat `_mended` flag).
2. **CANTRIP_TOUCH_EFFECTS registry pattern (Spare the Dying + Light — Session 11 implemented).** For non-attack, non-AoE, non-self-buff cantrips that target a single DOWNED ALLY or willing creature (Spare the Dying, Light v1): use the NEW `CANTRIP_TOUCH_EFFECTS` registry (handler signature `(caster, target, state) => boolean`). The dispatcher `resolveCantripTouchEffect(caster, target, actionName, state)` is consulted in `executePlannedAction`'s `case 'cast':` AFTER `resolveCantripAction` (self-buffs) and `resolveCantripAoE` (AoE), but BEFORE the target-null guard. CRITICAL: this routing MUST come BEFORE `if (!target || target.isDead || target.isUnconscious) break;` because Spare the Dying's target is UNCONSCIOUS (the standard guard would BLOCK unconscious targets). The handler itself decides whether the spell fires (Spare the Dying fizzles on monsters at 0 HP, on creatures above 0 HP, and on dead creatures; Light v1 fizzles on dead targets). If the cantrip name is registered but the target is null, the dispatcher returns true (the spell fizzles — the action is consumed but no effect is applied). The handler returns true whenever the cantrip name is registered (mirror `CANTRIP_AOE_EFFECTS`'s "spell is cast regardless" semantics). Future cantrips that target a single willing creature (Mending, future Help-action-like cantrips) should follow this pattern.
3. **Forward-compat scratch field pattern (Light + Mending — Session 11 implemented Light; Session 12 will implement Mending).** For cantrips whose v1 implementation sets a flag that the engine does NOT yet consume (Light's `_lightSourceActive` for the future vision subsystem; Mending's `_mended` for the future object-state subsystem): set the flag on the TARGET (not the caster) in `applyTouchEffect` (registered in `CANTRIP_TOUCH_EFFECTS`). v1 sets the flag but nothing reads it — documented via metadata flags (`lightVisionIntegrationV1Implemented: false`, `mendingConstructRepairV1Implemented: false`, etc.). The flag still clears at the start of the caster's NEXT turn via `cleanup()` called from `resetBudget` (v1 1-round simplification — canonically the spell lasts longer). Future work: extend the consuming subsystem (`computeLOS` for Light; a future object-state subsystem for Mending) to read the flag. This is the same forward-compat pattern as Guidance/Friends (whose flags await a future `rollAbilityCheck` choke point).
4. **Missing-choke-point scratch field pattern (Guidance + Friends — Session 11 implemented).** For self-buffs that grant a bonus to a roll type the engine doesn't yet have a choke point for (Guidance: +1d4 to ability checks; Friends: advantage on CHA checks): set a scratch flag on the CASTER in `applySelfEffect` (registered in `CANTRIP_SELF_EFFECTS`). v1 sets the flag but does NOT consume it (the future `rollAbilityCheck` choke point doesn't exist yet). The flag is cleared at the start of the caster's NEXT turn via `cleanup()` called from `resetBudget` (v1 1-round simplification). Documented via metadata flags (`guidanceAbilityCheckIntegrationV1Implemented: false`, `friendsAbilityCheckIntegrationV1Implemented: false`). Future work: add `rollAbilityCheck(combatant, ability, dc)` to `utils.ts` (mirror `rollSave`'s architecture — fold in `_guidanceDieBonusNextAbilityCheck` ADD + `_friendsAdvNextChaCheck` advantage, then consume). This is the same missing-choke-point pattern as Light/Mending (whose flags await a future vision/object-state subsystem).
5. **Metadata-only flavor-log self-buff pattern (Minor Illusion — Session 11 implemented; Session 12 will mirror for Mage Hand / Prestidigitation / Thaumaturgy / Message).** For cantrips whose v1 implementation needs NO rider, NO scratch field, and NO mechanical effect (Minor Illusion: creates a sound or image with no combat effect; Mage Hand: creates a spectral hand with no combat effect; Prestidigitation: creates a minor magical effect; Thaumaturgy: manifests a minor supernatural sign; Message: whispers a message): provide `metadata` only + an `applySelfEffect(caster, state) => boolean` that emits a SINGLE log event. NO scratch fields. NO new Combatant fields. NO CANTRIP_EFFECTS/TOUCH/AoE entries. Register in `CANTRIP_SELF_EFFECTS`. cleanup is a no-op. v1 simplifications are documented via metadata flags. This is the simplest cantrip pattern — v1 is essentially a "flavor action" that consumes the caster's action but has no mechanical effect. Future work: persistent-effect subsystems (remote-interaction for Mage Hand, multi-effect tracking for Prestidigitation/Thaumaturgy, communication for Message).
6. **CANON CORRECTION: Spare the Dying components are V+S (no M); Guidance components are V+S (no M); Friends components are S+M (makeup, no V); Light components are V+M (firefly/phosphorescent moss, NO S); Minor Illusion components are S+M (bit of fleece, no V).** All 5 cross-checked against the 5etools spell-cache JSON per the Session 9 protocol. The handover's component lists matched the canon JSON for all 5 (no corrections needed this session — Session 10 had to correct Gust and True Strike). **Future cantrip sessions should ALWAYS cross-check the handover's component list (and save ability, and range, and damage) against the 5etools spell-cache JSON before implementing** — the handover is a planning doc, the 5etools JSON is the authoritative source.
7. **CANON CORRECTION: Light has NO somatic component (V+M only).** The 5etools JSON for PHB p.255 lists `{"v":true,"m":"a firefly or phosphorescent moss"}` (NO `s:true`). This is unusual — most cantrips have a somatic component. The Session 11 module follows the canon JSON (V+M, NO S). Future cantrip sessions should not assume S is always present.
8. **`resolveCantripTouchEffect` target-null handling.** The dispatcher receives `target: Combatant | null` (the target may be null if `plan.targetId` is null or if `bf.combatants.get(plan.targetId)` returns undefined). If the cantrip name is registered but the target is null, the dispatcher returns true (the spell fizzles — the action is consumed but no effect is applied, mirror `CANTRIP_AOE_EFFECTS`'s "spell is cast regardless" semantics). This means the caller's `if (!target || ...) break;` guard is NOT reached for registered touch cantrips — the action is consumed by the dispatcher. CRITICAL: the caller must pass `target ?? null` (not just `target`) because `bf.combatants.get()` returns `Combatant | undefined`, but the dispatcher's signature is `Combatant | null`. The `?? null` coercion is in `combat.ts` line 780.
9. **`resetBudget` cleanup ordering.** The `resetBudget` function in `utils.ts` now calls 13 cantrip cleanup functions in order: Shield, RayOfFrost, ChillTouch, BladeWard, ViciousMockery, MindSliver, BoomingBlade, Frostbite, Infestation, Shillelagh, TrueStrike, Resistance, Guidance, Friends, Light. The order doesn't matter (cleanups are independent — each clears its own scratch field). Future cantrip sessions adding a new cleanup should append it to the end of the list (no need to interleave with existing cleanups).
10. **`eq()` takes 3 args (label, a, b), `assert()` takes 3 args (label, cond, detail).** Do NOT pass a detail string to `eq()` — it only takes `label, a, b` and the failure detail is auto-generated from JSON.stringify. Use `assert()` if you need a custom detail string on failure. The pattern `assert('label', x?.includes('Y') === true, true)` is WRONG — the third arg should be a string detail. Use `assert('label', x?.includes('Y') === true, 'got: ...')` or just `assert('label', !!x?.includes('Y'), 'got: ...')`. (Reiterated from Session 10 — still relevant.)
11. **Save-fail log casing (resolveAttack vs cantrip modules):** `resolveAttack`'s save branch (combat.ts) logs save_fail/save_success with LOWERCASE ability name (e.g. "fails DC 30 dex save") because it uses `${action.saveAbility}` (which is lowercase 'dex'/'con'/'str' etc.). Cantrip MODULES that emit their OWN save logs (Sword Burst, Thunderclap, Word of Radiance) use UPPERCASE ability name (e.g. "fails DEX save"). When testing a metadata-only cantrip that goes through resolveAttack's save branch (Create Bonfire, Acid Splash, Poison Spray, Gust, etc.), check for LOWERCASE. When testing a cantrip MODULE that emits its own save log (Sword Burst, Thunderclap, Word of Radiance), check for UPPERCASE. To avoid casing bugs, use `.toLowerCase().includes('dex')` for case-insensitive matching. (Reiterated from Session 10 — still relevant.)
12. **`isCritOverride=true` for deterministic attack-roll hits in tests.** +20 hitBonus vs AC 10 still has a 5% nat-1 auto-miss rate (PHB p.194). Use `isCritOverride=true` (5th arg to `resolveAttack`) to force a hit. Crit doubles the damage dice — adjust the damage range assertion accordingly (1d8 → 2d8 = 2..16, 1d10 → 2d10 = 2..20, 1d6 → 2d6 = 2..12). `isCritOverride=false` forces a miss. This is the attack-roll analogue of "DC=30 for guaranteed save fail." Established in Session 7, reused throughout Sessions 8/9/10/11. **None of the Session 11 cantrips have attack rolls** (Spare the Dying, Guidance, Friends, Light, Minor Illusion are all non-attack), so `isCritOverride` was NOT used in Session 11 tests. Session 12's batch is also all non-attack cantrips (Mage Hand, Prestidigitation, Thaumaturgy, Mending, Message — all utility), so `isCritOverride` will likely NOT be needed in Session 12 tests either.
13. **`JSON.stringify` for `eq()` deep equality on objects.** The `eq()` function uses `JSON.stringify` for the failure detail, but `===` for the actual comparison. For primitive comparisons (string, number, boolean), `===` works. For object comparisons, use `JSON.stringify(a) === JSON.stringify(b)` explicitly, or use `assert()` with a custom detail string. The pattern `eq('label', JSON.stringify(caster), before)` (used in the Minor Illusion "no scratch fields" test) works because both sides are stringified. For object equality, prefer `assert('label', objA.field === objB.field, 'detail')` over `eq('label', objA, objB)`. (Reiterated from Session 10 — still relevant.)
14. **Pre-existing flaky tests (do NOT try to fix — outside cantrip scope):** `combat.test.ts` (varying pass count 52–54, occasional 1 failure — d20-probabilistic), `faerie_fire.test.ts` (occasional 2 failures under suite load), `burning_hands.test.ts` (10% failure rate on nat 1-2). **Also previously observed:** `arms_of_hadar.test.ts` (occasional 2 failures, d20-probabilistic), `rage.test.ts` (occasional 1 failure, d20-probabilistic). All verified pre-existing by re-running on the Session 10 codebase without re-running cantrip tests — flakiness persists. NOT caused by cantrip work. The handover explicitly says do NOT fix these. **All 5 pre-existing flaky tests PASSED on the Session 11 final regression run** (82 test files, ~4937+ tests, all green).
15. **Build hygiene:** run `./node_modules/.bin/tsc --noEmit` before committing. Run the full suite: `for t in src/test/*.test.ts; do timeout 90 ./node_modules/.bin/ts-node --transpile-only "$t" 2>&1 | tail -3; done` (82 files, ~4937+ tests, must stay green except for the documented pre-existing flakiness). Revert test side-effects before committing: `git checkout -- characters/` if any fixture JSON got an `updatedAt` bump. **Session 11 followed this protocol:** characters/ reverted (one fixture JSON got an `updatedAt` bump from test side-effects — reverted before commit), all 82 test files green.

---

## IMMEDIATE NEXT ACTION

1. `git pull && npm install && npm run spell-cache:build` (confirm 35/49 cantrips implemented).
2. Implement the 5 cantrips in the order above (Mage Hand first — metadata-only flavor log, mirror Minor Illusion; Prestidigitation second — mirror Mage Hand but with multi-effect tracking TODO; Thaumaturgy third — mirror Prestidigitation but V-only; Mending fourth — NEW `_mended` scratch field via CANTRIP_TOUCH_EFFECTS, mirror Light v1; Message last — metadata-only flavor log, mirror Minor Illusion).
3. After each: `tsc --noEmit` + run that cantrip's test.
4. After all 5: run the **full regression suite** (must stay green; pre-existing flakiness in combat/faerie_fire/burning_hands/arms_of_hadar/rage is documented and NOT caused by cantrip work).
5. `npm run spell-cache:build` again — confirm cantrip implemented count goes 35 → 40.
6. Commit: `Cantrip-12: Implement Mage Hand, Prestidigitation, Thaumaturgy, Mending, Message (PHB)`.
7. Write `zHANDOVER-SESSION-13.md` (next batch — use `npm run spell-cache:pick -- --level 0 --count 5` to choose, or curate for mechanical diversity). After Session 12, 6 in-scope cantrips will remain: Control Flames (XGE), Dancing Lights (PHB), Druidcraft (PHB), Encode Thoughts (GGR), Mold Earth (XGE), Shape Water (XGE). Session 13 can implement all 6 (or 5 + reserve 1 for a follow-up batch).
8. **Push to GitHub.** Tell the user explicitly if the push fails.

---

## TEST STATUS (before this session — i.e. AFTER session 11)

- `spare_the_dying.test.ts`: 71/71 · `guidance.test.ts`: 52/52 · `friends.test.ts`: 53/53 · `light.test.ts`: 60/60 · `minor_illusion.test.ts`: 55/55
- Prior cantrip tests still green: `fire_bolt.test.ts` 43/43, `acid_splash.test.ts` 44/44, `poison_spray.test.ts` 46/46, `vicious_mockery.test.ts` 47/47, `sacred_flame.test.ts` 51/51, `blade_ward.test.ts` 38/38, `chill_touch.test.ts` 38/38, `shocking_grasp.test.ts` 26/26, `thorn_whip.test.ts` 11/11, `eldritch_blast.test.ts` 53/53, `toll_the_dead.test.ts` 61/61, `mind_sliver.test.ts` 48/48, `thunderclap.test.ts` 54/54, `booming_blade.test.ts` 218/218, `frostbite.test.ts` 57/57, `sword_burst.test.ts` 54/54, `sapping_sting.test.ts` 50/50, `lightning_lure.test.ts` 88/88, `green_flame_blade.test.ts` 209/209, `word_of_radiance.test.ts` 58/58, `produce_flame.test.ts` 52/52, `infestation.test.ts` 277/277, `shillelagh.test.ts` 60/60, `create_bonfire.test.ts` 99/99, `gust.test.ts` 74/74, `primal_savagery.test.ts` 57/57, `true_strike.test.ts` 49/49, `resistance.test.ts` 49/49, `magic_stone.test.ts` 61/61
- Full regression suite (82 files, ~4937+ tests): all green EXCEPT 5 pre-existing flaky tests (verified by re-running on the Session 11 codebase — all 5 PASSED on the final run, but they're documented as occasionally flaky):
  - `combat.test.ts`: varying pass count 52–54, occasional 1 failure (d20-probabilistic)
  - `faerie_fire.test.ts`: occasional 2 failures under suite load (d20-probabilistic)
  - `burning_hands.test.ts`: 10% failure rate on nat 1-2 (documented in prior handovers)
  - `arms_of_hadar.test.ts`: occasional 2 failures (d20-probabilistic)
  - `rage.test.ts`: occasional 1 failure (d20-probabilistic)
- `tsc --noEmit`: 0 errors
- Spell cache: 557 unique spells, **35/49 cantrips implemented**, 11 cantrips remaining in-scope (3 out-of-scope XPHB-only: Elementalism, Sorcerous Burst, Starry Wisp)

---

## NOTES FOR NEXT AGENT

- **Scope rule (per user):** canon pre-2024; reprints → newest in-scope source wins; XPHB (2024) out of scope. The cache handles this automatically — trust `spell-cache:pick`'s canonical-source column.
- **Creatures follow a different rule** (all variants kept) — not applicable to spells. See `SPELL-CACHE.md`.
- **Parser tech debt** (still open, documented in zHANDOVER-3/4/5/6/7/8/9/10/11): `hasMetalArmor` and `isUndead` flags exist on `Combatant` but aren't populated by the parser. `spellcastingMod` and `casterLevel` fields also exist but aren't populated by the parser. `isConstruct` does NOT exist yet (needed for Spare the Dying's canon type exclusion — currently a v1 simplification flag). Not blocking this batch.
- **AI planner** does not yet select most cantrips — engine routing is enough for this batch. AI selection is a Core Engine task.
- **Commit message convention:** `Cantrip-N: <summary>` (this session was Cantrip-11; next session is Cantrip-12).
- **Pre-existing flaky tests** (do NOT try to fix — outside cantrip scope, verified pre-existing): `combat.test.ts`, `faerie_fire.test.ts`, `burning_hands.test.ts`, `arms_of_hadar.test.ts`, `rage.test.ts`. These are d20-probabilistic and NOT caused by cantrip work.
- **Architecture summary (5 cantrip registries, 26 cantrips total after Session 11):**
  - `CANTRIP_EFFECTS` (13) — post-hit / post-save-fail riders: Thorn Whip, Ray of Frost, Shocking Grasp, Chill Touch, Vicious Mockery, Mind Sliver, Booming Blade, Frostbite, Sapping Sting, Lightning Lure, Green-Flame Blade, Infestation, Gust
  - `CANTRIP_ATTACK_ADVANTAGE` (1) — pre-roll advantage: Shocking Grasp
  - `CANTRIP_SELF_EFFECTS` (7) — self-buffs: Blade Ward, Shillelagh, True Strike, Resistance, Guidance, Friends, Minor Illusion
  - `CANTRIP_AOE_EFFECTS` (3) — caster-centered AoE: Thunderclap, Sword Burst, Word of Radiance
  - `CANTRIP_TOUCH_EFFECTS` (2) — non-attack touch-effect on a single target: Spare the Dying, Light
  - (Primal Savagery + Magic Stone are metadata-only — no registry entry.)
- **Exported for testability:** `resolveAttack` (Session 6), `executeMove` (Session 7), `rollRandomDirection` / `directionToDelta` / `isDestinationBlocked` / `applyRandomMove` (Session 9 — Infestation), `pushAway` / `canPushSize` (Session 10 — Gust), `resolveCantripTouchEffect` (Session 11 — NEW, for testing touch-effect cantrip routing). All have doc comments noting they're for direct testing of cantrip engine integration.
- **New `Combatant` fields added in Session 11:** `_guidanceDieBonusNextAbilityCheck?: number`, `_friendsAdvNextChaCheck?: boolean`, `_lightSourceActive?: boolean`, `_isStabilized?: boolean`. Optional with sensible defaults.
- **CANTRIP_TOUCH_EFFECTS routing architecture (Spare the Dying + Light):** For non-attack, non-AoE, non-self-buff cantrips that target a single DOWNED ALLY or willing creature: use the `CANTRIP_TOUCH_EFFECTS` registry (handler signature `(caster, target, state) => boolean`). The dispatcher `resolveCantripTouchEffect(caster, target, actionName, state)` is consulted in `executePlannedAction`'s `case 'cast':` AFTER `resolveCantripAction` (self-buffs) and `resolveCantripAoE` (AoE), but BEFORE the target-null guard. CRITICAL: this routing MUST come BEFORE `if (!target || target.isDead || target.isUnconscious) break;` because Spare the Dying's target is UNCONSCIOUS. The handler itself decides whether the spell fires. If the target is null, the dispatcher returns true (fizzle, action consumed). Distinct from `CANTRIP_SELF_EFFECTS` (no target) and `CANTRIP_AOE_EFFECTS` (caster-centered multi-target).
- **Forward-compat scratch field architecture (Light):** Set a scratch flag on the TARGET (not the caster) in `applyTouchEffect` (CANTRIP_TOUCH_EFFECTS). v1 sets the flag but the consuming subsystem (computeLOS for Light) does NOT yet read it — documented via metadata flags. The flag still clears at the start of the caster's NEXT turn via `cleanup()` called from `resetBudget` (v1 1-round simplification). Future work: extend the consuming subsystem to read the flag.
- **Missing-choke-point scratch field architecture (Guidance + Friends):** Set a scratch flag on the CASTER in `applySelfEffect` (CANTRIP_SELF_EFFECTS). v1 sets the flag but does NOT consume it (the future `rollAbilityCheck` choke point doesn't exist yet). The flag is cleared at the start of the caster's NEXT turn via `cleanup()` called from `resetBudget` (v1 1-round simplification). Future work: add `rollAbilityCheck(combatant, ability, dc)` to `utils.ts` (mirror `rollSave`'s architecture — fold in `_guidanceDieBonusNextAbilityCheck` ADD + `_friendsAdvNextChaCheck` advantage, then consume).
- **Metadata-only flavor-log self-buff architecture (Minor Illusion):** Provide `metadata` only + an `applySelfEffect(caster, state) => boolean` that emits a SINGLE log event. NO scratch fields. NO new Combatant fields. NO CANTRIP_EFFECTS/TOUCH/AoE entries. Register in `CANTRIP_SELF_EFFECTS`. cleanup is a no-op. v1 simplifications are documented via metadata flags. This is the simplest cantrip pattern — v1 is essentially a "flavor action." Session 12 will mirror this pattern for Mage Hand, Prestidigitation, Thaumaturgy, and Message.

---

## AGENT PROTOCOL (PERPETUAL)

- **REPOSITORY:** https://github.com/mcabel/dnd-combat-sim
- **COMMIT POLICY:** Always `git add` and `git commit` the `zHANDOVER-SESSION-*.md` file to the project root directory. **MUST UPLOAD CODE/ARTIFACTS/ETC TO THE GITHUB REPO AFTER THE ZHANDOVER IS PRODUCED.** Tell the user explicitly if a push fails.
- **OUTPUT POLICY:**
  - PRIORITY: Upload/commit code/artifacts to the GitHub repo.
  - ONLY output the handover in the chat if access to the repo is somehow blocked.
  - NO summaries, no conversational filler, no "Here is the file" headers.
